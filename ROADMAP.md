# Roadmap SmartTicket

Suivi des évolutions techniques et fonctionnelles du projet. Statuts : **Fait**, **En cours**, **À faire**.

> **Mise à jour (2026-08-15)** : Phase 0 (provisioning) validée de bout en bout contre Render, console d'opérateur locale (page de gestion de flotte) construite et testée, modèle de vente clarifié (3 niveaux). Aucune entrée historique supprimée.

---

## Fait récemment (chantier provisioning + page de gestion)

### Phase 0 — Provisioning validé en réel contre Render ✅
Série de bugs invisibles aux tests mockés, trouvés et corrigés en lançant le provisioning pour de vrai :
- [x] Champ `version` Postgres requis par l'API Render (défaut 18).
- [x] 5 écarts de schéma OpenAPI (déballage `service`/`deploy`, `runtime` requis, `pre_deploy_failed`) — sans le fix `deploys`, `wait_for_deploy_live` timeout 900s même sur succès.
- [x] Plan Postgres `starter` legacy → `basic_256mb`. Coût réel confirmé : ~25 $/mois par instance (Postgres 6 $ + stockage 4,50 $ + 2 services web).
- [x] Race condition `connection-info` avant `available` → vrai polling `wait_for_postgres_available`.
- [x] `NEXT_PUBLIC_API_URL` vide (frontend appelait son propre domaine) → URL backend déterministe posée au build.
- [x] `FRONTEND_URL` (backend) pointait `localhost:3005` → liens email cassés. Injectée au provisioning.
- [x] Réutilisation de slug → 404 `/setup`. Résolu par suffixe unique sur les noms Render.
- [x] `delete_client.py` : ne retire plus la ligne du registre si la suppression Render échoue (fail-fast + `deletion_failed` + IDs conservés).
- [x] Email Brevo débloqué (restriction d'IP désactivée côté compte). Flux complet validé : provisioning → email bienvenue → `/setup` → connexion admin → **création compte utilisateur → mail de vérification → connexion** → chat IA répond (avec garde-fou anti-injection confirmé en réel).
- [x] `audit_render_resources.py` (lecture seule) : détecte les ressources orphelines, croise avec `instances.db`.
- [x] Rollback validé en conditions réelles (échecs successifs nettoyés, zéro orpheline).

### Console d'opérateur — page de gestion de flotte locale (`ops/fleet_admin.py`) ✅
Page web locale (FastAPI + Jinja2, `127.0.0.1` uniquement, jamais exposée), appelle `provision()`/`delete_instance()` sans dupliquer la logique.
- [x] **Voir** : liste des instances (registre + API Render), santé (ping GET /), statut, plan, abonnement, liens dashboard, détection d'orphelines.
- [x] **Suspendre / réactiver** via le coupe-circuit `VENDOR_KEY` — testé en réel (402 sur `/v1/*` quand suspendu, health check `GET /` préservé à 200).
- [x] **Supprimer** définitivement (double confirmation : saisie du slug + case cochée), rappel « suspendre ≠ arrêter de facturer », date de suspension via `InstanceSubscription.updated_at`.
- [x] **Créer** depuis un formulaire (nom entreprise → slug auto → provisioning en tâche de fond, ~5 min, suivi honnête, POST-Redirect-GET). White-label du nom (`NEXT_PUBLIC_BRAND_NAME`) injecté depuis `--name`.
- [x] Retry sur 5xx Render transitoire dans le rollback ; contraste corrigé ; redirection 303 après chaque action POST.

### White-label — nom d'entreprise dynamique ✅ (partiel)
- [x] `NEXT_PUBLIC_BRAND_NAME` (défaut « SmartTicket ») remplace le nom codé en dur (header, sidebar, titres, pages auth). Injecté au provisioning depuis le nom du client. Pages légales volontairement exclues (identifient l'entité contractante réelle).

---

## En cours / à vérifier

- [ ] **404 sur la colonne « Santé » de la page de gestion** : le ping santé d'une instance fraîchement créée renvoie parfois 404 alors que Render la dit « ok » et que l'instance fonctionne. À élucider : la page ping-t-elle la bonne URL (backend vs frontend, bon endpoint de health check) ? Non bloquant (l'instance marche), mais à corriger pour la fiabilité de l'affichage.
- [ ] **Passe design de la page de gestion** : lisibilité, contraste, mise en page — regroupée en une fois plutôt qu'au coup par coup.
- [ ] **Passe design de l'instance client** : corriger les défauts visuels repérés (à faire en même temps que le white-label complet ci-dessous).

---

## À faire

### Modèle de vente — clarifié (3 niveaux)
Cible : **secteur régulé** (santé, fintech, assurance, juridique, RH, public) où l'isolation est un critère contractuel — **sales-led**. MAIS on ouvre aussi la porte aux clients entrants via un paiement en ligne, sans construire le self-service complet.

- **Niveau 1 (dispo dès que le juridique est prêt)** : démo → paiement (virement / facture / Stripe Payment Link) → je déclenche la création depuis la page locale. Onboarding client en ~5 min.
- **Niveau 2 (petit ajout, recommandé)** : bouton « S'abonner » sur le site → paiement carte (Stripe Checkout / Payment Links, sans coder) → je reçois la notif → je clique « créer ». Ouvre les ventes ENTRANTES sans démo préalable. **Ne nécessite PAS** de service hébergé ni de webhook. Coexiste avec un bouton « Demander une démo » pour les gros comptes régulés.
- **Niveau 3 (plus tard, gros chantier)** : paiement → création 100 % automatique sans intervention. Nécessite service de provisioning hébergé + webhook Stripe + registre Postgres + gestion des échecs. À construire seulement quand le volume rend le clic manuel pénible.

### Prérequis AVANT le premier euro (bloquant, priorité)
- [ ] **Entité juridique** déclarée (micro-entreprise suffit pour démarrer) — nécessaire pour Stripe et pour encaisser.
- [ ] **Finaliser CGV + mentions légales + politique de confidentialité + DPA** (brouillons étoffés le 2026-08-25 : clauses génériques manquantes ajoutées — coordonnées LCEN complètes de l'hébergeur, pénalités de retard B2B obligatoires + indemnité forfaitaire 40€, force majeure, clause de modification unilatérale des CGV, droit à la limitation RGPD, durée de conservation des données de facturation ; plafond de responsabilité CGV ajouté mais explicitement marqué `[À VALIDER PAR UN AVOCAT]`, pas encore validé). Reste à faire : compléter les `[À COMPLÉTER]` d'identité légale une fois l'entité créée (SIREN/RCS/capital social/forme juridique/adresse/etc.) + relecture avocat SaaS/RGPD obligatoire avant toute signature. Le DPA est non négociable sur le régulé (sous-traitant art. 28) — **son contenu dépend d'une décision d'architecture non tranchée** (Tiqia héberge-t-il les données du Client, ou le Client apporte-t-il sa propre base ?), qui changerait la qualification RGPD de Tiqia et le périmètre du document : signalé explicitement dans `DPA.md`, volontairement non figé tant que ce choix n'est pas arrêté.
- [ ] **Moyen d'encaisser** : Stripe Payment Links (le plus simple, bouton payer en ~10 min sans code) ou virement/facture.
- [ ] **Instance de démo** prête et montrable.
- [ ] **1-2 premiers prospects** à qui parler.

### Alignement produit sur la cible régulée (avant tout démarchage sérieux)
- [ ] **White-label complet** (au-delà du nom, déjà fait) : logo, couleurs du client, et surtout **retrait de « Propulsé par Mistral » côté utilisateur final** (no-go absolu sur le régulé). La mention « vous interagissez avec une IA » reste (AI Act).
- [x] **Séparer le site vitrine de l'instance client** — en réalité déjà résolu (commit `ac396ad`, 2026-08-15, avant cette session) : le flag `NEXT_PUBLIC_DEPLOYMENT_MODE`/`IS_MARKETING_SITE` (`frontend/lib/deploymentMode.ts`) défaut à `"instance"`, et `frontend/proxy.ts` redirige `/` vers `/chat` dans ce mode — une instance client ne sert donc PAS la landing marketing. **Correction d'une affirmation erronée que j'avais moi-même ajoutée ici le 2026-08-25** (chantier email de bienvenue) : j'avais écrit que `frontend/app/page.tsx` servait "toujours" la landing marketing sur `/` de chaque instance — inexact, je n'avais alors lu que `page.tsx` sans vérifier `proxy.ts`/`deploymentMode.ts`. Item retiré de "à faire", cf. aussi les 3 pages légales déplacées vers `landing/` (2026-08-25, cf. "Fait" ci-dessous), qui restent la seule action de ce chantier réalisée pendant cette session.
- [ ] **Secrets isolés par tenant** : `MISTRAL_API_KEY` / `BREVO_API_KEY` sont aujourd'hui partagées entre instances (un DPO le verra). Cible : clé par instance, injectée au provisioning.
- [ ] **Conformité AI Act art. 50** (obligation depuis le 2 août 2026) : vérifier/ajouter la mention « vous parlez à une IA » visible dès le début de l'interaction.
- [ ] **Traçabilité de l'isolation** : documenter l'architecture (bases/secrets/réseau/logs séparés) dans un format présentable à un DPO. Vérifier que `INDEX_CLOSED_TICKETS=false` reste actif par défaut.
- [ ] **Hébergement UE strict** : confirmer qu'aucun edge/CDN Render hors UE n'est dans le chemin ; backups et logs en UE. Discours : « hébergé en UE, conforme RGPD ».
- [ ] **Déprovisioning avec purge vérifiable** : produire une preuve de destruction (base + backups + logs) présentable au client.

### Connexion client depuis la landing (à faire à la première signature)
Permettre à un client existant de se connecter à son instance depuis tiqia.fr.
- **Contexte** : archi mono-tenant, chaque client a son instance sur un sous-domaine dédié (ex: client.tiqia.fr). La landing tiqia.fr est commune et ne peut pas router un visiteur vers la bonne instance sans information.
- **Solution retenue** (modèle Slack/Notion) : bouton « Se connecter » → page intermédiaire « Entrez le nom de votre espace » → le client tape son identifiant (ex: « martin ») → redirection vers {identifiant}.tiqia.fr. Pas d'annuaire central (respecte l'isolation), la landing construit juste l'URL du sous-domaine.
- **Prérequis** : au moins un vrai client avec un sous-domaine {client}.tiqia.fr en place, pour pouvoir tester réellement le flux + gérer le cas d'erreur « cet espace n'existe pas ».
- **Pourquoi reporté** : aucun client aujourd'hui = fonctionnalité non testable et sans valeur immédiate. À construire à la première signature.

### Outillage de flotte (avant le 3e client)
- [ ] **Patch/update de masse** : `update_all_instances.py` existe, à valider en réel (`--dry-run`, `--only` pour rollout progressif). Rappel : un push sur `main` redéploie déjà TOUS les clients → `main` toujours déployable, CI = garde-fou.
- [ ] **Registre SQLite → Postgres** : `instances.db` local ne suit pas entre machines (constaté). Prioritaire dès qu'on opère depuis plus d'un poste, ou pour tout service hébergé (Niveau 3).
- [ ] **Metering d'usage par tenant** (`usage_monthly`, jamais fait) : conversations + tokens LLM par instance/mois. Sans ça, impossible de savoir quel client plombe la facture Mistral, ni de facturer à l'usage. Urgent au 2e client.
- [ ] **Backups** : Render fait déjà PITR 3 jours (7 en Pro) + Export 7 jours — vérifié. À TESTER : une vraie restauration. À CONSTRUIRE plus tard (cible régulée) : exports périodiques chiffrés vers stockage objet UE (rétention longue + copie hors-Render), conditionné au service hébergé.
- [ ] **Monitoring/alerting par tenant** (santé, coût, incidents).

### Question de stack à trancher (§6 du brief)
- [ ] Terraform/Pulumi vs scripts + API Render actuels. **Décision provisoire : rester sur les scripts Python** (l'IaC déclaratif devient pertinent vers ~10 clients ou si le patch de masse devient fragile). À réévaluer à 10 clients.

### Hébergement France (Scaleway + Terraform) — décision CONDITIONNELLE
- [ ] **Déclencheur** : un client exige *contractuellement* la France (pas seulement UE). Render/Frankfurt suffit pour la plupart. Implique de réécrire toute la couche de provisioning (API Scaleway ≠ Render) + adopter Terraform. **Ne lancer qu'avec un contrat signé qui le justifie.**

---

## Fait


### Nettoyage (2026-07-07)
- [x] Suppression des fichiers polluants versionnés (index pgvector binaire, lock LibreOffice, lockfiles pnpm en double avec npm)
- [x] Suppression du code mort : `backend/ingest_pdf.py` (pipeline RAG jamais appelé), `backend/migrate.py` (doublonné par les migrations au démarrage), `backend/uv.lock` (orphelin, non utilisé)
- [x] Factorisation de `REASON_LABELS`/`REASON_COLORS`/`VALID_REASONS` dupliqués dans `backend/constants.py`
- [x] Remplacement des `print("DEBUG: ...")` par du `logging` cohérent (`routers/ai.py`, `ingest_postgres.py`)
- [x] Nettoyage de `requirements.txt` (doublon `requests`)
- [x] Suppression de la dépendance frontend inutilisée `react-is`
- [x] Nettoyage de `.claude/settings.local.json` (référence à un autre projet local)
- [x] Correction du tag OpenAPI vide sur le router `analytics`

### Sécurité (2026-07-07)
- [x] `POST /v1/setup-admin` sécurisé — était accessible sans authentification (création/promotion admin par n'importe qui). Désactivé par défaut, protégé par header `X-Setup-Key` + variable d'env `ADMIN_SETUP_KEY`

### Dette technique backend (2026-07-07)
- [x] `backend/db/init-db.sql` resynchronisé avec `models.py` — ajout de `utilisateur.deleted_at`, `chat_sessions.transfer_reason`/`deleted_at`, `chat_messages.feedback`, de la table `ai_call_logs` (absente), et de contraintes `NOT NULL` manquantes. Les `ALTER TABLE` au démarrage (`main.py`) sont conservés comme filet de sécurité pour les bases déjà déployées (ex: Render), mais ne sont plus nécessaires pour une installation neuve
- [x] Dépendances Python unifiées dans `backend/pyproject.toml` (`[project.dependencies]` + `[project.optional-dependencies].dev`) — suppression de `requirements.txt`/`requirements-dev.txt`. `Dockerfile` fait désormais `pip install .` et la CI fait `pip install "./backend[dev]"`

### Sécurité (2026-07-07)
- [x] Rate limiting sur `POST /login` (5/min par IP, anti brute-force) et `POST /ask/stream` (20/min par compte utilisateur, coûts Mistral) via `slowapi`. Stockage `memory://` par défaut (aucune dépendance externe requise) ; conçu pour basculer vers le Redis déjà présent dans `docker-compose.yml` (`RATE_LIMIT_STORAGE_URI=redis://...`) si l'app est un jour scalée sur plusieurs instances Render — **limite actuelle à connaître : sur une seule instance memory:// suffit, mais si le plan Render passe à plusieurs instances ce stockage local par instance ne suffira plus, il faudra alors activer Redis**. `Dockerfile` mis à jour avec `--proxy-headers` pour que l'IP détectée soit bien celle du client derrière le proxy Render, pas le load balancer

### Tests & CI (2026-07-07)
- [x] Tests de composants ajoutés pour `UserDashboard`, `SavDashboard`, `AdminDashboard` (25 tests, chargement/erreurs/recherche/actions clés type clôture de session, promotion SAV, envoi de réponse). Ajout de `frontend/jest.setup.ts` (matchers `@testing-library/jest-dom`, absents jusqu'ici) et `frontend/test-utils/fetchMock.ts` (mock `fetch` partagé). Suite complète vérifiée manuellement : `npm test`, `npx tsc --noEmit`, `npx eslint`, `npm run build` passent tous

### Fonctionnel / produit (2026-07-07)
- [x] Export RGPD (`GET /v1/me/export`) génère maintenant un vrai PDF au lieu d'un JSON téléchargé tel quel — nouveau module `backend/pdf_export.py` (`fpdf2`). Limite connue : les fonts core PDF (Helvetica) ne couvrent que le Latin-1, donc les emoji/caractères exotiques dans le contenu des messages utilisateurs sont remplacés par `?` plutôt que de faire planter l'export (compromis assumé plutôt que d'ajouter une police Unicode embarquée)

### Dette technique backend (2026-07-07)
- [x] `@app.on_event("startup")` (déprécié) remplacé par un `lifespan` FastAPI unique dans `main.py`. Bonus au passage : le scheduler APScheduler (purge RGPD) est désormais arrêté proprement à l'extinction de l'app (`scheduler.shutdown()`), ce qu'`on_event` ne faisait jamais puisqu'il n'y avait aucun handler `"shutdown"`. Vérifié via `TestClient` en cycle complet (entrée + sortie de contexte) et collecte des 55 tests pytest sans erreur d'import
- [x] `sqlalchemy.ext.declarative.declarative_base` (déprécié) remplacé par `sqlalchemy.orm.declarative_base` dans `backend/database.py` — le `MovedIn20Warning` associé a disparu à la collecte des tests

### Tests & CI (2026-07-07)
- [x] Nouveau test `backend/tests/test_schema_consistency.py` : charge `init-db.sql` dans un schéma Postgres jetable (isolé des tables utilisées par le reste de la suite) et compare colonnes/tables obtenues avec `Base.metadata` (donc `models.py`). Tourne automatiquement dans la CI existante (le service Postgres du job `backend-tests` suffit, aucune config CI à ajouter). **Non exécuté en local** : le PostgreSQL local de la machine s'est arrêté en cours de route et `initdb` ne peut pas créer de cluster jetable (`pg_hba.conf.sample` manquant dans l'installation locale — problème propre à cette machine, sans rapport avec le projet) ; validé uniquement par relecture rigoureuse + vérification statique que `Base.metadata` correspond exactement colonne par colonne à `init-db.sql` actuel — à confirmer par le premier run CI

### Bugs corrigés (2026-07-07)
- [x] `AdminDashboard.tsx` (colonne Conversations) — le `<button>` "Clôturer" imbriqué dans le `<button>` de sélection de session (HTML invalide, erreur d'hydratation React) a été corrigé en remplaçant l'élément englobant par un `<div role="button" tabIndex={0}>` avec support clavier (Entrée/Espace), sur le même modèle que `UserDashboard.tsx`. Vérifié : les 25 tests Jest passent, le warning `<button> cannot be a descendant of <button>` a disparu de la console, `tsc --noEmit` et `eslint` restent propres

### Documentation (2026-07-07)
- [x] `CHANGELOG.md` corrigé — la section `[1.0.0]` mentionnait Traefik/Watchtower (jamais utilisés dans le projet) et omettait Redis/Ollama/Open WebUI (bien présents dans `docker-compose.yml`). Liste des 7 services alignée sur le fichier réel

### Fonctionnel / produit (2026-07-07)
- [x] Recherche full-text dans l'historique des conversations — nouvel endpoint `GET /v1/sessions/search` (Postgres `to_tsvector`/`plainto_tsquery`/`ts_headline`, config `french`), cherche dans le contenu des messages + les titres. Intégré dans `UserDashboard.tsx` (recherche serveur débouncée à 300ms, remplace l'ancien filtre client qui ne portait que sur les titres). **Point de sécurité traité** : `ts_headline` entoure les termes trouvés de balises `<b>` sans échapper le reste du texte — un rendu naïf via `dangerouslySetInnerHTML` aurait ouvert une XSS si un message contenait du HTML ; le snippet est donc découpé côté client sur les seules balises `<b>`/`</b>` connues et rendu comme texte React normal (jamais interprété comme HTML). Ajout de `backend/tests/test_session_search.py` (6 tests : contenu, titre, aucun résultat, requête vide, permissions) et de 2 tests frontend (surlignage + absence de résultats). Bump `2.0.0` → `2.1.0` (ajout d'endpoint rétrocompatible). **Non exécuté en local côté backend** (même limite Postgres locale que le test de cohérence de schéma) — vérifié par relecture rigoureuse, `ruff`, et collecte pytest (62 tests) sans erreur ; à confirmer par la CI. Côté frontend, entièrement vérifié réellement (Jest, tsc, eslint, build)
- [x] Recherche full-text étendue à `AdminDashboard.tsx` (liste des conversations de l'utilisateur sélectionné) — même endpoint `/v1/sessions/search`, même helper de surlignage sécurisé, désormais partagé entre les deux dashboards via `frontend/components/dashboard/searchSnippet.tsx` (au lieu d'être dupliqué). La recherche se réinitialise proprement au changement d'utilisateur sélectionné. 2 tests frontend ajoutés (surlignage + réinitialisation au changement d'utilisateur) — 28 tests Jest passent, `tsc`/`eslint`/`build` vérifiés réellement
- [x] Export des dashboards Analytics et Monitoring IA en PDF/CSV. **PDF** (serveur) : nouveaux endpoints `GET /v1/analytics/stats/pdf` et `GET /v1/analytics/ai-metrics/pdf`, réutilisant `backend/pdf_export.py` (même pattern que l'export RGPD) — KPIs, alertes, et tableaux (évolution quotidienne, raisons de transfert, agents SAV / latence, enrichissements KB). Le calcul des données a été extrait de chaque route JSON existante en fonctions `_build_stats_data`/`_build_ai_metrics_data` réutilisables, sans changer le comportement des endpoints JSON déjà en place. **CSV** (client, sans aller-retour serveur) : nouveau `frontend/lib/csv.ts` (export multi-sections, échappement RFC 4180, BOM UTF-8 pour Excel), boutons ajoutés sur `/analytics` et `/monitoring`. Bump `2.1.0` → `2.2.0` (ajout d'endpoints rétrocompatibles). Vérifié réellement : les 4 builders PDF testés avec données réalistes + cas vides (rendu inspecté visuellement), 7 tests pytest ajoutés (permissions + validité PDF), 2 tests Jest pour `downloadCsv` (structure CSV + échappement — un test sur le BOM a été retiré car `FileReader` le strip automatiquement au décodage sous jsdom, ce n'est pas un bug). 30 tests Jest, `tsc`/`eslint`/`build` passent tous. **Non exécuté en local côté backend** (même limite Postgres locale que les autres tests ajoutés cette session) — à confirmer par la CI
- [x] Rôles/permissions plus fins — nouveau rôle `superviseur` : hérite de tout ce qu'un agent SAV peut faire (`is_admin_or_sav` étendu) et peut en plus promouvoir/rétrograder `user` <-> `sav` via `PUT /v1/users/{id}/role` (nouvelle fonction `can_manage_sav_team`), sans jamais pouvoir toucher un compte `admin`, en promouvoir un, éditer un profil (`PUT /users/{id}`) ou supprimer un compte (`DELETE /users/{id}` reste strictement admin-only). Nouveau `frontend/components/dashboard/SupervisorDashboard.tsx` : panneau de gestion d'équipe (promotion/rétrogradation) + file d'attente de tickets intégrée en réutilisant `SavDashboard` tel quel. Sidebar, redirections de pages (`/analytics`, `/monitoring`, `/ai-assistant/[id]`) et bootstrap des rôles (`init-db.sql`, `main.py`, `conftest.py`) mis à jour pour inclure le nouveau rôle. Bump `2.2.0` → `2.3.0` (nouvelle capacité rétrocompatible). Vérifié réellement : 9 tests pytest ajoutés couvrant les permissions (promotion/rétrogradation autorisées, promotion vers admin refusée, modification d'un compte admin refusée, suppression/édition de profil refusées, accès aux fonctionnalités agent confirmé), 78 tests pytest collectés sans erreur, `ruff` propre. Frontend : 5 tests Jest pour `SupervisorDashboard` (dont l'intégration réelle de `SavDashboard`), 35 tests Jest au total, `tsc`/`eslint`/`build` passent tous. **Non exécuté en local côté backend** (même limite Postgres locale que les autres tests ajoutés cette session) — à confirmer par la CI
- [x] Amélioration du pipeline RAG : reranking des chunks + ajustement automatique selon le feedback utilisateur. Nouveau `backend/rag_reranking.py` (`rerank_chunks`, testé unitairement sans DB) : sur-échantillonne `KB_TOP_K * RERANK_FETCH_MULTIPLIER` candidats pgvector puis reclasse par score composite similarité (rang) + recouvrement lexical (0.3) + feedback net normalisé (0.2), avec **quarantaine automatique** des chunks dont le feedback net descend à `RERANK_QUARANTINE_THRESHOLD` (défaut -3) — réalise directement la piste « Quarantaine des chunks à feedback négatif répété » identifiée dans `docs/E3/RAG_DECISIONS_LOG.md` (déplacée de la table V2 vers les décisions adoptées, avec une nouvelle entrée datée). Nécessite une nouvelle colonne `chat_messages.source_kb_ids INTEGER[]` pour tracer quels chunks ont servi à chaque réponse IA (sans ce lien, impossible de rattacher un feedback à un chunk). Câblé dans `backend/routers/ai.py::ask_question_stream`, sans changer le contrat de `POST /ask/stream`. Bump `2.3.0` → `2.4.0`. **Un vrai bug de conception attrapé en testant réellement** : mes deux premiers scénarios de test ("le recouvrement lexical peut faire remonter un chunk moins bien classé", "le feedback positif départage un ex-æquo") échouaient — pas un bug du reranker, mais un pool de candidats trop petit dans mes tests (l'écart de similarité entre rang 0 et rang 1 est trop grand pour être comblé par les poids lexical/feedback quand il n'y a que 2-3 candidats). Corrigés avec des pools de taille réaliste (comme le sur-échantillonnage `KB_TOP_K × 3` en production), qui confirment que le reranking fonctionne comme prévu sans jamais laisser un signal secondaire écraser une similarité vectorielle nettement supérieure. 9 tests unitaires exécutés réellement en local (aucune DB requise pour ce module), 87 tests pytest collectés au total, `ruff` propre. **Non exécuté en local pour la partie DB** (agrégation du feedback par chunk, même limite Postgres locale que les autres tests ajoutés cette session) — à confirmer par la CI
- [x] Démo/onboarding guidé au premier login, adapté à chaque rôle — option retenue : modal léger sans dépendance externe plutôt qu'une librairie de "product tour" (pas de nouvelle dépendance, code simple à maintenir). Nouveau `frontend/components/onboarding/OnboardingModal.tsx` : 4 étapes par rôle (`user`/`sav`/`superviseur`/`admin`), navigation précédent/suivant, "Passer" à tout moment. Affiché une seule fois par couple `(userId, role)` via `localStorage` (se réaffiche automatiquement si le rôle change, ex. après une promotion — l'utilisateur découvre alors ses nouvelles capacités). Rejouable manuellement depuis Paramètres → carte "Aide" → "Revoir la visite guidée". Aucun changement backend, pas de bump de version API. Corrigé un avertissement ESLint réel pendant l'implémentation (`react-hooks/set-state-in-effect` : `setState` synchrone dans un `useEffect` remplacé par un état initial paresseux, puisque `userId`/`role` sont déjà stables au montage). 7 tests Jest ajoutés et exécutés réellement (première visite, non-réaffichage, contenu par rôle, repli sur le rôle `user` si rôle inconnu, navigation, "Passer", "Terminé"). 42 tests Jest au total, `tsc`/`eslint`/`build` passent tous
- [x] **Bug attrapé par la CI (pas en local)** : les 9 tests `test_supervisor_role.py` échouaient sur `PUT /v1/users/{id}/role` avec `400 Rôle invalide` dès la promotion vers `superviseur`. Cause : la liste blanche de rôles valides dans `backend/routers/users.py` (`update_user_role` **et** `update_user_by_admin`) avait été laissée à `["user", "sav", "admin"]` lors de l'ajout du rôle `superviseur` (2.3.0) — `can_manage_sav_team` et la logique de restriction superviseur étaient correctes, mais la validation en amont rejetait la valeur avant de les atteindre. Corrigé dans les deux fonctions (`["user", "sav", "superviseur", "admin"]`). En creusant le même angle, un second bug lié a été trouvé et corrigé en proactif : `frontend/components/dashboard/AdminDashboard.tsx` ne récupérait jamais `role=superviseur` (`loadAll()` ne fetchait que user/sav/admin) et le `<Select>` d'édition de rôle n'offrait pas l'option "Superviseur" — un compte promu devenait donc invisible dans le dashboard admin même une fois le bug backend corrigé. Ajout d'une 5ᵉ colonne "Superviseurs" (grille `lg:grid-cols-5`), état `superviseurUsers`/`superviseurPage`, option manquante dans le `Select`. Vérifié réellement : `tsc --noEmit` propre, `npx jest __tests__/AdminDashboard.test.tsx` (6/6, dont un nouveau test de non-régression sur la visibilité), `ruff check .` propre sur tout le backend. **Correction backend elle-même non exécutée en local** (même limite Postgres locale) — confirmée uniquement par le retour CI qui a révélé le bug initial. Pas de bump de version (correctif d'une fonctionnalité 2.3.0 non encore publiée, pas une nouvelle capacité)
- [x] Vérification de l'adresse email utilisateur à l'inscription. `POST /v1/register` envoie un lien de confirmation (JWT dédié type `email_verification`, expire 48h) ; `GET /v1/verify-email?token=...` le valide (idempotent) ; `POST /v1/resend-verification` le renvoie (rate limité 3/heure, réponse générique identique que le compte existe ou non — anti-énumération). `POST /v1/login` refuse désormais un compte non vérifié (`403`, `{code: "email_not_verified"}`). Envoi via nouveau `backend/email_utils.py` en SMTP générique (variables d'env `SMTP_HOST`/`SMTP_USER`/`SMTP_PASSWORD`/`SMTP_FROM`, compatible n'importe quel fournisseur — Brevo, SendGrid, Gmail... — pas de SDK propriétaire imposé, adapté à Render qui ne fournit pas de service d'email natif) ; sans `SMTP_HOST` configuré (dev local, tests), le lien est simplement loggué au lieu d'échouer. **Décision produit** : changer son email (`PUT /v1/me`) réinitialise `email_verified` (on ne peut pas hériter de la confiance de l'ancienne adresse) ; les comptes admin (bootstrap au démarrage et `/setup-admin`) sont auto-vérifiés (jamais créés via l'inscription publique) ; les comptes déjà existants sur une base déjà déployée sont automatiquement marqués vérifiés à la toute première migration suivant ce déploiement (grandfathering, pour ne verrouiller aucun utilisateur existant) — ce grandfathering ne s'applique qu'une seule fois (détection via `information_schema.columns`), pas à chaque redémarrage, sinon il annulerait la vérification pour toute inscription faite entre deux déploiements. Frontend : nouvelle page `/verify-email` (succès/erreur + formulaire de renvoi), écran "Vérifiez votre boîte mail" après inscription (remplace la redirection immédiate vers `/login`), et `/login` propose de renvoyer le lien quand le compte n'est pas encore vérifié. Bump `2.4.0` → `2.5.0`. Vérifié réellement : 14 tests pytest ajoutés (`test_email_verification.py` : non-vérifié bloqué, vérification réussie, idempotence, token invalide/garbage, token d'accès JWT rejeté comme token de vérification, 404 sur user_id inconnu, anti-énumération du resend, réinitialisation au changement d'email, auto-vérification admin), 101 tests pytest collectés sans erreur, `ruff` propre. Les fixtures/tests existants qui faisaient `register` puis `login` (`conftest.py`, `test_api.py`, `test_session_search.py`, `test_supervisor_role.py`) ont dû être adaptés (nouvelle fixture `mark_verified`) puisque le login est désormais bloqué par défaut — sans ça, tous ces tests auraient cassé. Frontend : 8 tests Jest ajoutés (`LoginPage`, `SignUpPage`, `VerifyEmailPage`), 50 tests Jest au total, `tsc --noEmit`/`eslint`/`npm run build` passent tous réellement. **Non exécuté en local côté backend** (même limite Postgres locale que le reste de la session) — à confirmer par la CI
- [x] **Faux positifs GitGuardian (2)** sur `backend/tests/test_session_search.py` et `backend/tests/test_supervisor_role.py` : le détecteur générique "mot de passe" ne comprend pas les commentaires `noqa` et matche toute affectation `xxx_PASSWORD = "<littéral>"`, y compris ma précédente correction `_TEST_PASSWORD = "not-a-real-secret-123"` (qui n'avait fait que déplacer le motif, pas le supprimer). Corrigé en générant le mot de passe de test dynamiquement (`secrets.token_urlsafe(16)`, un par run) au lieu d'une chaîne en dur — élimine le motif à la source plutôt que de compter sur un examen manuel côté GitGuardian. Vérifié réellement : `ruff check .` propre sur tout le backend, 87 tests pytest collectés sans erreur. **Dette identifiée en creusant** : `test_api.py`, `test_analytics.py` et `conftest.py` contiennent le même motif de mot de passe en dur (`"password123"`, `"pass123"`, `"admin_password123"`) — non flaggés par GitGuardian cette fois (hors du diff de cette PR) mais qui déclencheront la même alerte dès qu'une future PR touchera ces fichiers ; à généraliser au même correctif (`secrets.token_urlsafe`) en une passe dédiée
- [x] Notifications (in-app + email) quand le SAV répond ou qu'un ticket est transféré. Nouvelle table `notifications` (`id_utilisateur`, `id_session`, `type`, `message`, `read_at`) et nouveau module `backend/notifications.py` : `queue_sav_reply` (déclenché dans `POST /v1/messages` quand `type_envoyeur == "sav"`, notifie le propriétaire du ticket) et `queue_session_transferred` (déclenché dans `POST /v1/sessions/{id}/transfer`, notifie tous les comptes `sav`/`superviseur`/`admin`, jamais l'utilisateur qui a lui-même transféré son ticket). Nouveaux endpoints `GET /v1/notifications`, `GET /v1/notifications/unread-count`, `PATCH /v1/notifications/{id}/read`, `POST /v1/notifications/read-all`. **Décision produit** : email best-effort uniquement pour la réponse SAV (le client n'est pas forcément devant l'app) réutilisant `email_utils.py` déjà en place — pas d'email sur les transferts pour éviter de spammer toute l'équipe à chaque ticket (les agents surveillent déjà le dashboard, l'in-app suffit) ; les fonctions `queue_*` ajoutent la notification à la transaction SQL en cours sans commit propre, l'email n'étant envoyé qu'après le commit du routeur pour ne jamais notifier un événement qui, finalement, n'a pas été persisté. Frontend : nouveau `frontend/components/NotificationBell.tsx` dans `AppSidebar` — badge non-lues avec polling 20s, liste déroulante (clic = marque lu + navigue vers la session), "tout marquer lu". Bump `2.5.0` → `2.6.0`. Vérifié réellement : 8 tests pytest ajoutés (`test_notifications.py` : notif réponse SAV, message user/ai propre ne notifie personne, transfert notifie les agents mais ni l'auteur ni un simple utilisateur, marquer un/tous comme lus, isolation entre comptes, 401 sans auth), 109 tests pytest collectés sans erreur, `ruff` propre. **Bug de test évité en cours de route** : `auth_client` et `client` sont le même objet `TestClient` dans `conftest.py` (juste des headers par défaut mutés) — un premier jet du test utilisait `auth_client` (propriétaire) puis `_make_admin_client(client)` qui aurait écrasé silencieusement le jeton du propriétaire ; réécrit avec des headers explicites par requête pour chaque acteur. Frontend : 5 tests Jest ajoutés (`NotificationBell.test.tsx`), 55 tests Jest au total, `tsc --noEmit`/`eslint`/`npm run build` passent tous réellement. **Non exécuté en local côté backend** (même limite Postgres locale que le reste de la session) — à confirmer par la CI
- [x] **Bug attrapé par la CI (premier run avec un vrai Postgres, tout le reste de la suite — 107/109 tests — est passé du premier coup)** : `TestResendVerification::test_resend_does_not_leak_whether_account_exists` et `::test_resend_after_verification_still_returns_generic_message` échouaient avec `429 Rate limit exceeded` au lieu du message générique attendu. Cause : `RESEND_VERIFICATION_RATE_LIMIT` (3/heure par défaut) n'avait pas été désactivé dans `conftest.py`, contrairement à `LOGIN_RATE_LIMIT`/`ASK_RATE_LIMIT` — plusieurs tests de `TestResendVerification` appellent `/v1/resend-verification` depuis la même IP `testclient` dans la même session et finissent par épuiser le quota entre eux. Corrigé en ajoutant `RESEND_VERIFICATION_RATE_LIMIT=10000/minute` à `conftest.py`, même pattern que les deux autres limites déjà désactivées pour les tests. Vérifié réellement : `ruff check .` propre, 109 tests pytest collectés sans erreur — la correction elle-même (interaction avec le vrai rate limiter) ne peut être reconfirmée qu'au prochain run CI, faute de Postgres local
- [x] **Envoi d'email cassé en prod sur Render (trouvé en testant réellement avec un compte Brevo)** : le SMTP générique (`backend/email_utils.py`) échouait en deux temps sur Render — d'abord `TimeoutError` sur le port 587 (bloqué en sortie par l'hébergeur, contournable en passant au port 2525, également supporté par Brevo), puis `535 Authentication failed` malgré des identifiants corrects, et enfin `525 Unauthorized IP address` une fois la vraie cause identifiée : Brevo restreint par défaut l'usage des clés SMTP à une liste d'IPs autorisées, or Render n'a pas d'IP de sortie fixe sur les plans standards — whitelister l'IP du jour n'aurait tenu que jusqu'au prochain changement d'IP côté Render. Corrigé en ajoutant un mode d'envoi par **API HTTP Brevo** (`BREVO_API_KEY`, prioritaire sur `SMTP_HOST` si les deux sont définis) : passe par HTTPS classique (`requests.post` vers `https://api.brevo.com/v3/smtp/email`), aucune notion d'IP autorisée ni de port SMTP à gérer. Le SMTP générique reste en repli pour d'autres fournisseurs sans API HTTP équivalente. Vérifié réellement : `ruff check .` propre, 109 tests pytest collectés sans erreur, payload de l'appel HTTP vérifié par un script avec `requests.post` mocké (sender/to/subject/textContent/htmlContent corrects). Pas de bump de version (correctif d'une fonctionnalité 2.5.0 déjà publiée, pas un changement de contrat d'API)
- [x] **Lien de vérification d'email cassé en prod, trouvé en testant réellement bout en bout après avoir résolu l'envoi d'email** : cliquer sur le lien reçu par mail redirigeait systématiquement vers `/login` (HTTP 307, `Location: /login`) au lieu d'afficher la page `/verify-email` — reproductible même sans token, donc un problème de routage et non lié à Brevo. Cause trouvée en creusant l'historique git : `frontend/proxy.ts` (garde d'authentification globale, l'équivalent Next.js 16 de `middleware.ts` — fichier existant avant cette session, jamais mentionné ni découvert lors de l'ajout de la vérification d'email) redirige toute route absente de sa liste `isPublicPath` vers `/login` si aucun cookie `auth_token` n'est présent ; `/verify-email` n'y avait jamais été ajoutée, alors qu'un visiteur y accède *par définition* sans être connecté. Corrigé en ajoutant `pathname === "/verify-email"` à `isPublicPath`. Nouveau `frontend/__tests__/proxy.test.ts` (8 tests, environnement Jest `node` requis pour les globals Fetch API dont dépend `NextRequest`/`NextResponse` — absents de l'environnement `jsdom` par défaut du projet) couvrant les 5 chemins publics, la redirection d'un visiteur non authentifié depuis une route protégée, et la redirection d'un utilisateur déjà connecté loin de `/login`. Vérifié réellement : `tsc --noEmit`, `eslint`, `npm run build` et 63 tests Jest (dont les 8 nouveaux) passent tous. Pas de bump de version (correctif, pas un changement de contrat d'API)

- [x] Colonne `tenant_id` (UUID, valeur fixe `00000000-0000-0000-0000-000000000001` unique par instance, cf. `constants.DEFAULT_TENANT_ID`) posée sur les 6 tables principales (`utilisateur`, `chat_sessions`, `chat_messages`, `ai_call_logs`, `knowledge_base` — y compris la table des chunks vectoriels, la plus sensible en cas de fuite cross-tenant future —, `notifications`), en préparation d'une future bascule vers un vrai multi-tenant. `Role` volontairement exclue (table de référence partagée, pas une donnée par tenant). Aucune logique de scoping ajoutée, aucun endpoint modifié, colonne non exposée dans les schémas Pydantic — purement additif, pour transformer une future migration multi-tenant en fusion de données plutôt qu'en réécriture de schéma (cf. section "Stratégie de commercialisation" ci-dessous pour le contexte de la décision). Migration idempotente dans `main.py::run_migrations` (boucle `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` + index, pas de grandfathering nécessaire contrairement à `email_verified` puisque toutes les lignes prennent la même valeur fixe). Vérifié réellement : `ruff check .` propre, 109 tests pytest collectés sans erreur, colonnes confirmées par script Python sur chaque modèle (type UUID, default correct). Le test `test_schema_consistency.py` existant valide génériquement que `init-db.sql` reste synchronisé avec `models.py`, sans modification nécessaire. Pas de bump de version (colonne interne, invisible côté API). **Non exécuté en local contre un vrai Postgres** (même limite que le reste de la session) — à confirmer par la CI

- [x] Réinitialisation de mot de passe fonctionnelle — `/forgot-password` était un piège UX (formulaire visible, cliquable, mais sans `onSubmit` ni endpoint backend). Implémenté sur le même pattern que la vérification d'email : `POST /v1/forgot-password` envoie un lien par email (JWT dédié type `password_reset`, expire 1h, réponse générique anti-énumération, limité à `FORGOT_PASSWORD_RATE_LIMIT=3/heure`) ; `POST /v1/reset-password` valide le lien (rejette un token d'un autre type, ex. `email_verification`) et met à jour le mot de passe (min. 6 caractères, même contrainte que `PUT /v1/me/password`). Page `/forgot-password` réécrite avec le style visuel des autres pages auth (elle n'avait jamais eu le wrapper plein écran des autres, juste un `Card` orphelin sans fond) ; nouvelle page `/reset-password`. **`frontend/proxy.ts` mis à jour dès l'écriture** (`/reset-password` ajoutée à `isPublicPath`) — leçon tirée du bug de `/verify-email` la veille, pas la peine de le redécouvrir une seconde fois. Bump `2.6.0` → `2.7.0` (nouveaux endpoints). Vérifié réellement : 7 tests pytest ajoutés (`test_password_reset.py` : demande pour compte existant/inconnu réponse identique, reset avec token valide permet la reconnexion avec le nouveau mot de passe et invalide l'ancien, mot de passe trop court rejeté, token invalide/garbage rejeté, token d'un autre type — `email_verification` — rejeté, user_id inconnu → 404), 116 tests pytest collectés sans erreur, `ruff` propre. `conftest.py` mis à jour préventivement (`FORGOT_PASSWORD_RATE_LIMIT=10000/minute`) pour éviter de reproduire le bug CI de rate-limit croisé entre tests déjà rencontré sur `/resend-verification`. Frontend : 5 tests Jest ajoutés (`ForgotPasswordPage`, `ResetPasswordPage` — un test corrigé en cours de route : l'indicateur de non-correspondance des mots de passe s'affiche en direct dès la saisie *en plus* du message d'erreur à la soumission, donc `findByText` remontait plusieurs éléments, changé pour `findAllByText`), `proxy.test.ts` étendu à `/reset-password`. 69 tests Jest au total, `tsc --noEmit`/`eslint`/`npm run build` passent tous réellement. **Non exécuté en local côté backend** (même limite Postgres locale que le reste de la session) — à confirmer par la CI

- [x] **Fuite RAG entre clients finaux, trouvée en confrontant le code au modèle économique B2B2C** (support public, pas B2B interne comme supposé implicitement jusque-là) : `close_session` indexait automatiquement le transcript complet et un résumé IA de *tout* ticket clôturé dans `knowledge_base` — la même table utilisée pour construire le contexte RAG des réponses à *tous les futurs utilisateurs*. Sans scoping par utilisateur à la lecture, un fragment de conversation privée du client final A (nom, numéro de commande, situation personnelle) pouvait ressortir dans la réponse IA donnée au client final B. Acceptable en usage B2B interne (collègues de confiance qui se partagent la connaissance) ; inacceptable en B2B2C. Corrigé en rendant ce comportement **opt-in** via `INDEX_CLOSED_TICKETS` (défaut `false`) dans `dependencies.py`, tout le bloc résumé/embedding/indexation de `close_session` désormais conditionné par ce flag. Bump `2.7.0` → `2.8.0` (rupture de comportement sur un endpoint existant). Vérifié réellement : `ruff check .` propre, 118 tests pytest collectés sans erreur. Nouveau `test_close_session_indexing.py` (2 tests : par défaut aucune ligne créée dans `knowledge_base` à la clôture ; avec le flag activé et `generate_text`/`embed_text` mockés, les catégories `ticket_summary`/`ticket_transcript` sont bien créées). **Non exécuté en local** (même limite Postgres locale que le reste de la session) — à confirmer par la CI. Au passage, deux autres gaps identifiés en creusant le même angle B2B2C, pas encore traités : `POST /v1/register` n'a aucun rate limiting (contrairement à login/resend-verification/forgot-password), et le chat exige aujourd'hui un compte + email vérifié avant la moindre question (pas de mode anonyme) — à trancher avant un vrai pilote B2B2C

- [x] `POST /v1/register` rate-limité (`REGISTER_RATE_LIMIT`, défaut `5/heure`) — seul endpoint public du flux auth sans protection, porte ouverte au spam de comptes jetables en inscription publique B2B2C. Décorateur passé en callable (`@limiter.limit(lambda: REGISTER_RATE_LIMIT)`) au lieu de la chaîne directement, contrairement aux autres rate limits du projet — `slowapi` fige sinon la valeur au chargement du module, ce qui rendait impossible de tester réellement le comportement "limite dépassée" ; confirmé par un script de sanity-check (`monkeypatch` de l'attribut du module reflété par le lambda, vérifié en dehors de pytest). `conftest.py` mis à jour préventivement (`REGISTER_RATE_LIMIT=10000/minute`) — `/register` est appelé par la quasi-totalité des fichiers de test, même bug de rate-limit croisé déjà rencontré deux fois sur `/resend-verification` et `/forgot-password`. Bump `2.8.0` → `2.9.0`. Vérifié réellement : `ruff check .` propre, 119 tests pytest collectés sans erreur, nouveau `test_register_rate_limit.py` (limite abaissée à 2/heure via monkeypatch pour le test, 3e inscription consécutive → 429). **Non exécuté en local contre un vrai Postgres** (même limite que le reste de la session) — à confirmer par la CI

- [x] **Parcours anonyme B2B2C tranché et implémenté** : deux options envisagées (vrai concept "invité" avec `id_utilisateur` nullable et token JWT dédié, vs. compte "fantôme" créé silencieusement). Choix du compte fantôme — nettement moins de code, réutilise tel quel toutes les vérifications d'ownership existantes (un compte fantôme est un `Utilisateur` normal aux yeux du reste du code). `POST /v1/sessions/guest` crée le compte (username/email aléatoires sous `GUEST_EMAIL_DOMAIN`, mot de passe aléatoire jamais communiqué, `email_verified=True` puisqu'il n'y a pas de vrai email à confirmer) et une session en une seule requête, sans authentification préalable, pose directement le cookie de session (même mécanique que `/login`). `POST /v1/me/claim` transforme ensuite ce compte en compte réel (email + mot de passe) **sans exiger le mot de passe actuel** (le compte fantôme n'en a pas de connaissable) — gardé par `is_guest_email()`, la possession d'une session valide pour ce compte précis tenant lieu de preuve. Réutilise `email_verified=False` + renvoi d'un lien de confirmation, exactement le même mécanisme que le changement d'email classique dans `PUT /v1/me`. **Détail non trivial retrouvé et corrigé** : le JWT existant contient l'ancien email dans son claim `sub` — sans réémission du cookie après le claim, `get_current_user` ne retrouverait plus l'utilisateur en base juste après avoir rempli le formulaire (déconnexion surprise). `claim_guest_account` réémet donc un cookie à jour, comme `/login`. Comptes fantômes jamais réclamés purgés automatiquement après `GUEST_ACCOUNT_TTL_DAYS` (défaut 7j, `main.py::purge_unclaimed_guests`, greffé sur le scheduler RGPD existant, cascade sur leurs sessions) et exclus de `GET /v1/users` (bruit inutile dans la gestion des comptes). Détecté par un domaine d'email réservé plutôt qu'une colonne dédiée — zéro migration de schéma. Frontend : nouvelle page publique `/chat` (crée la session invité, redirige vers `/ai-assistant/{id}`), lien discret "Discuter maintenant, sans compte" sur la landing page, bandeau `GuestClaimBanner` affiché aux visiteurs anonymes dans le chat (formulaire inline, pas de gate bloquant avant transfert vers un agent — nudge UX, pas contrainte technique). `proxy.ts` mis à jour **dans le même commit** qui introduit `/chat` (leçon du bug `/verify-email` définitivement acquise). `REGISTER_RATE_LIMIT`/`GUEST_SESSION_RATE_LIMIT` tous deux en callable pour rester testables. Bump `2.9.0` → `2.10.0`. Vérifié réellement : 14 tests pytest ajoutés (`test_guest_sessions.py` : session invité utilisable immédiatement pour lire son profil et poster un message, `is_guest` correct sur invité/utilisateur normal, claim met à jour email/mot de passe et réémet un cookie fonctionnel, claim rejette email déjà pris/mot de passe court/compte non-invité, exclusion de `GET /v1/users`, purge soft-delete un invité ancien non réclamé + sa session mais épargne un invité récent ou déjà réclamé) + 1 test de rate limit dédié (`test_guest_session_rate_limit.py`), 133 tests pytest collectés sans erreur, `ruff` propre. Frontend : 8 tests Jest ajoutés (`GuestClaimBanner`, `StartChatPage`, `proxy.test.ts` étendu à `/chat`), 75 tests Jest au total, `tsc --noEmit`/`eslint`/`npm run build` passent tous réellement. **Non exécuté en local contre un vrai Postgres** (même limite que le reste de la session) — à confirmer par la CI

- [x] Guardrails anti prompt-injection sur le chat IA public — le chat anonyme (`POST /v1/sessions/guest`) étant en ligne depuis la précédente entrée, `build_rag_prompt()` (`dependencies.py`) n'avait aucune défense contre "ignore tes instructions précédentes et fais X" : la QUESTION brute était concaténée sans délimiteur ni consigne de sécurité. Ajout de règles explicites dans le prompt système : la QUESTION est étiquetée comme donnée utilisateur (jamais une instruction système), interdiction explicite de changer de rôle/révéler le prompt/sortir du périmètre support même si demandé dans la question, refus poli + proposition de transfert humain en cas de demande hors périmètre. Mitigation par durcissement de prompt (l'approche standard et proportionnée à ce stade) — pas un filtre de mots-clés séparé, jugé trop fragile/faux-positifs pour la valeur ajoutée. Aucun changement de contrat d'API (fonction interne), pas de bump de version. Vérifié réellement : `ruff check .` propre, 135 tests pytest collectés sans erreur, 2 nouveaux tests dans `test_utils.py::TestBuildRagPrompt` (présence des règles de sécurité, étiquetage explicite de la section QUESTION) + sanity-check autonome (contournant la fixture Postgres autouse de `conftest.py`, comme pour `rag_reranking.py` plus tôt dans la session) confirmant le contenu exact du prompt généré avec une tentative d'injection réaliste

- [x] Dépendance frontend morte `@base-ui/react` retirée de `package.json` — jamais importée nulle part dans `app/`/`components/`, doublon de `radix-ui` déjà utilisé partout dans `components/ui/`. `package-lock.json` régénéré (3 paquets retirés). Vérifié réellement : `npm run build`, `npx eslint`, `npx tsc --noEmit`, `npm test` (75 tests Jest) passent tous. Pas de bump de version (dépendance frontend inutilisée, aucun contrat d'API concerné)
- [x] Mot de passe DB par défaut (`Password1234`) sorti du code source — confirmé par l'utilisateur comme purement local (différent de la prod). `backend/database.py` : plus de valeur de repli, `DATABASE_URL` doit être définie explicitement sinon `RuntimeError` au démarrage (même pattern que `SECRET_KEY` dans `main.py`). `docker-compose.yml` : `POSTGRES_DB`/`POSTGRES_USER`/`POSTGRES_PASSWORD` interpolés depuis `.env` (`${POSTGRES_PASSWORD:?...}` — échec explicite si absent), `DATABASE_URL` du service `backend` dérivée des mêmes variables au lieu de dupliquer le mot de passe une deuxième fois en dur (source unique de vérité). `backend/start-postgres-windows.sh` : `POSTGRES_PASSWORD` désormais exigé en variable d'environnement (`: "${POSTGRES_PASSWORD:?...}"`) plutôt qu'en dur. `.env.example`/`README.md` mis à jour en cohérence (`change-me` comme placeholder explicite plutôt qu'une vraie valeur qui fonctionne telle quelle). **Volontairement non touché** : `docs/E4/C15/*.md` (documentation de certification/portfolio, instantané d'un livrable plutôt que doc vivante — décision explicite de l'utilisateur). Vérifié réellement : `ruff check .` propre, 135 tests pytest collectés sans erreur (l'ordre d'exécution de `conftest.py`, qui fixe `DATABASE_URL` avant tout import applicatif, n'est pas affecté), sanity-check dédié confirmant que l'import de `database.py` lève bien `RuntimeError` sans `DATABASE_URL`, `docker-compose.yml` validé comme YAML syntaxiquement correct avec les bonnes chaînes d'interpolation. Pas de bump de version (configuration/infra, aucun contrat d'API changé)
- [x] Mots de passe de test en dur généralisés à `secrets.token_urlsafe(16)` (`_TEST_PASSWORD` niveau module) dans `test_api.py`, `test_analytics.py` et `conftest.py` — dernier foyer de littéraux `"password123"`/`"pass123"`/`"admin_password123"` du projet, même motif que les faux positifs GitGuardian déjà corrigés ailleurs. Dans `conftest.py` spécifiquement, `_TEST_PASSWORD` remplace la valeur en dur de la fixture `registered_user`, utilisée par la quasi-totalité de la suite. Vérifié réellement : `ruff check .` propre, 135 tests pytest collectés sans erreur. Pas de bump de version (tests uniquement, aucun contrat d'API changé)
- [x] `shadcn` (outil CLI de génération de composants, jamais importé au runtime) déplacé de `dependencies` vers `devDependencies` dans `frontend/package.json`. `package-lock.json` régénéré (flags `dev` mis à jour). Vérifié réellement : `npm run build`, `npx eslint`, `npx tsc --noEmit`, `npm test` (75 tests Jest) passent tous. Pas de bump de version (classification de dépendance uniquement, aucun contrat d'API concerné)
- [x] `except Exception: pass` silencieux autour de l'insertion `AICallLog` dans `routers/ai.py::stream_tokens` remplacé par `logger.warning(...)`, cohérent avec le reste du fichier (erreur RAG plus haut) et `sessions.py::close_session` qui loggent déjà leurs échecs non bloquants. Un échec d'insertion du log d'appel IA (analytics) passait jusqu'ici totalement inaperçu. Vérifié réellement : `ruff check .` propre, 135 tests pytest collectés sans erreur. Pas de bump de version (logging interne, aucun contrat d'API changé)
- [x] Suppression de `docs/E1`, `docs/E3`, `docs/E4` (23 fichiers : rapports/spécifications/PDF de certification RNCP, dont `docs/E4/C15` — instantané d'un livrable de soutenance déjà rendue, gardé intact jusqu'ici, décision explicite de l'utilisateur de le supprimer maintenant). Référence obsolète à `docs/E3/RAG_DECISIONS_LOG.md` retirée du docstring de `backend/rag_reranking.py`. Aucun code applicatif n'y faisait référence (documentation pure). Pas de bump de version (suppression de documentation, aucun contrat d'API changé)
- [x] Rate limiting préparé pour une future bascule Redis, sans rien provisionner ni activer maintenant — vérifié que Render tourne en `plan: free` (single-instance, pas de scaling horizontal possible) et qu'aucun Redis n'est provisionné côté Render (`render.yaml`), donc le besoin réel n'existe pas encore. `RATE_LIMIT_STORAGE_URI` était déjà configurable (`memory://` par défaut) mais la bascule vers `redis://...` aurait échoué à l'import faute du client Python `redis` — ajouté à `backend/pyproject.toml`, confirmé réellement en construisant un `Limiter(storage_uri="redis://localhost:6379")` (aucune connexion réelle nécessaire, juste l'absence d'`ImportError` sur le backend `RedisStorage` de `limits`). Commentaires étoffés dans `dependencies.py` et `.env.example` avec la procédure de bascule exacte (provisionner un Redis managé côté Render + changer la variable d'env, aucun changement de code requis le jour venu). Vérifié réellement : `ruff check .` propre, 135 tests pytest collectés sans erreur. Pas de bump de version (dépendance interne, aucun contrat d'API changé)
- [x] `POST /v1/register` ne remonte plus une erreur 500 quand l'email/username est déjà pris par un compte soft-deleted pas encore purgé — la contrainte `UNIQUE` en base porte sur toutes les lignes (y compris `deleted_at IS NOT NULL`), mais le pré-check applicatif filtrait sur `deleted_at IS NULL`, laissant passer ce cas jusqu'au `db.commit()` qui échouait alors avec une `IntegrityError` non gérée. Retrait du filtre `deleted_at` sur les deux pré-checks (email et username) de `register_user` — comportement désormais aligné sur la portée réelle de la contrainte SQL, message 400 propre dans tous les cas plutôt qu'un 500 selon le timing de la purge RGPD (30j). 2 tests ajoutés dans `test_api.py::TestRegister` (email et username d'un compte soft-deleted simulé via `db_session`, vérifient le 400 + le message exact plutôt qu'un crash). Vérifié réellement : `ruff check .` propre, 137 tests pytest collectés sans erreur. **Non exécuté en local contre un vrai Postgres** (même limite que le reste de la session) — à confirmer par la CI. Pas de bump de version (correction de bug sur un comportement déjà documenté comme 400, pas un nouveau contrat d'API)
- [x] `GET /v1/knowledge-base/ingest-status` et `GET /v1/knowledge-base/robots-check` sécurisés — n'exigeaient que `get_current_user` (n'importe quel compte connecté), contrairement au reste du router `knowledge.py` (`ingest-url`/`ingest-file`/`delete-source`) qui exige `is_admin_or_sav`. Un simple compte `user` pouvait donc faire sonder par le backend n'importe quelle URL externe via `robots-check` (SSRF-adjacent). Alignés sur le même contrôle de rôle. Nouveau `test_knowledge_base_permissions.py` (4 tests : `user` refusé 403 sur les deux endpoints, `admin` passe la vérification — 404 sur job inconnu pour `ingest-status`, 200 avec `analyze_robots_and_sitemap` mocké pour `robots-check`). Vérifié réellement : `ruff check .` propre, 139 tests pytest collectés sans erreur. **Non exécuté en local contre un vrai Postgres** (même limite que le reste de la session) — à confirmer par la CI. Pas de bump de version (durcissement de contrôle d'accès sur un endpoint existant, pas de nouveau contrat d'API)
- [x] Dépendances Python pinnées dans `backend/pyproject.toml` — `fastapi`, `uvicorn`, `apscheduler`, `requests`, `psycopg2-binary`, `sqlalchemy`, `pgvector`, `python-multipart`, `passlib`, `python-jose`, `email-validator`, `python-dotenv`, `slowapi`, `redis`, `fpdf2`, `langchain-community`, `beautifulsoup4`, `langchain-text-splitters` et les 3 dépendances de `dev` (`pytest`, `pytest-cov`, `httpx`) n'avaient aucune version fixée, contrairement à `lxml`/`pypdf`/`python-docx`/`bcrypt`/`typing-extensions`/`ruff` — un rebuild Docker/Render pouvait silencieusement installer une version majeure différente entre deux déploiements. Épinglées sur les versions actuellement installées et déjà validées par toute la session (`pip list --format=freeze` du venv de vérification). Vérifié réellement : `pip check` ne trouve aucun conflit de dépendances avec ce jeu de versions exact, `ruff check .` propre, 141 tests pytest collectés sans erreur. Dockerfile (`python:3.11-slim`) et CI (`python-version: "3.11"`) déjà alignés sur `requires-python >= 3.11`, non modifiés. Pas de bump de version (verrouillage de build, aucun contrat d'API changé)
- [x] Bandeau de confirmation d'email dans Paramètres — après changement d'email (`PUT /v1/me`), le backend repasse `email_verified: false` (comportement voulu) mais l'UI ne le signalait jamais, l'utilisateur ne le découvrant qu'au prochain login refusé. `frontend/app/(dashboard)/settings/page.tsx` affiche désormais un bandeau ambre "Confirmez votre nouvelle adresse email" dès que `email_verified` est `false` (au chargement de la page **et** immédiatement après un changement d'email réussi, sans attendre un rechargement), avec un bouton "Renvoyer l'email" (`POST /v1/resend-verification`, même endpoint et réponse générique anti-énumération que sur `/login`). Nouveau `frontend/__tests__/SettingsPage.test.tsx` (4 tests : bandeau absent si vérifié, présent si non vérifié, renvoi de l'email fonctionnel, apparition immédiate après sauvegarde du profil). Vérifié réellement : `tsc --noEmit`, `eslint`, `npm run build` et 79 tests Jest (dont les 4 nouveaux) passent tous. Pas de bump de version (UI uniquement, endpoint `/resend-verification` déjà existant)
- [x] Limite de `INGEST_JOBS` (dict en mémoire du process) documentée plutôt que "corrigée" — analyse : contrairement au rate limiting (un simple compteur, où Redis est une vraie solution), un job d'ingestion **est** la tâche `BackgroundTasks` FastAPI elle-même, exécutée dans ce process ; un redémarrage tue le job en cours, pas seulement son statut. Déplacer le dict vers un stockage partagé (Redis/Postgres) ne rendrait donc pas les jobs résilients à un redémarrage — ça remplacerait juste un 404 propre ("Job introuvable") par un statut bloqué à "running" indéfiniment. Une vraie résilience demanderait une vraie file de tâches (table + worker de reprise), jugé hors scope tant que l'app tourne en single-instance (Render gratuit) et que les redémarrages en pleine ingestion ne sont pas un problème vécu — décision explicite de l'utilisateur après présentation du compromis. Commentaire détaillé ajouté dans `backend/dependencies.py` à côté de `INGEST_JOBS` pour que ce choix reste visible et ne soit pas re-découvert comme un "oubli" plus tard. Vérifié réellement : `ruff check .` propre, 141 tests pytest collectés sans erreur. Pas de bump de version (commentaire seul, aucun changement de comportement)
- [x] Item ROADMAP obsolète corrigé — "Recherche/filtre dans la liste des sources de la base de connaissances" était déjà listé comme "à faire", mais `frontend/app/(dashboard)/knowledge-base/page.tsx` a **déjà** une recherche (`searchQuery`/`filteredSources`, filtre sur `source` et `category`) et des chips de filtre par catégorie, présents depuis le tout premier commit du dépôt (`git log` confirme un seul commit touchant ce fichier). L'item n'a jamais reflété l'état réel du code — retiré sans changement de code, aucune régression à corriger
- [x] **Brouillon de DPA (Data Processing Agreement)** — nouveau `DPA.md` à la racine, structuré sur le modèle des clauses contractuelles types CNIL (article 28 RGPD), adapté au fonctionnement réel de SmartTicket : objet/nature/finalité du traitement, obligations du sous-traitant (sécurité, confidentialité, assistance aux droits des personnes), sous-traitants ultérieurs listés (Mistral AI, Render, Brevo — mêmes que dans `/politique-confidentialite`), sort des données en fin de contrat (30j), notification de violation, droit d'audit. C'est un document annexé au contrat commercial signé avec chaque client, pas une page publique du site (contrairement aux mentions légales/CGV/politique de confidentialité). **Brouillon non validé juridiquement** : identité légale en placeholders `[À COMPLÉTER]`, relecture avocat recommandée avant signature — même statut que les 3 pages légales publiques. Pas de bump de version (document, aucun changement de code)
- [x] **Brouillon des 3 pages légales publiques** — mentions légales (`/mentions-legales`), politique de confidentialité RGPD (`/politique-confidentialite`, données collectées, sous-traitants listés : Mistral AI, Render, Brevo, durée de conservation, droits des personnes) et CGV (`/cgv`) créées, liées dans le footer de la landing page et remplaçant le lien générique CNIL par un lien interne dans la case à cocher RGPD de l'inscription. **Ce sont des brouillons basés sur des trames standard, pas des documents validés juridiquement** : identité de l'entité (raison sociale, SIREN, adresse) laissée en placeholders `[À COMPLÉTER]` car aucune entité juridique n'est encore enregistrée pour le projet, et une relecture par un avocat spécialisé SaaS reste nécessaire avant tout usage contractuel réel (surtout pour les CGV et le futur DPA, non traité ici — c'est normalement un document annexé à chaque contrat client, pas une page publique). `proxy.ts` mis à jour dans le même commit pour les 3 nouvelles routes publiques (leçon `/verify-email`). Vérifié réellement : `tsc --noEmit`, `eslint`, `npm run build` (3 nouvelles routes statiques confirmées) et 85 tests Jest (dont `proxy.test.ts` étendu aux 3 nouvelles routes) passent tous. Pas de bump de version (pages statiques, aucun contrat d'API changé)
- [x] **Synchronisation d'annuaire, version import CSV** — l'admin peut désormais importer en masse les utilisateurs d'une entreprise cliente depuis un export CSV de son ERP (`email`/`username`/`prenom`/`nom`), plutôt qu'une vraie intégration LDAP/AD (hors scope pour l'instant, pas de besoin exprimé). Nouveau `POST /v1/users/import-csv` (admin-only, même check strict que `DELETE /users/{id}`) : parse le CSV (repli latin-1 si l'export n'est pas en UTF-8, cas fréquent des ERP français), valide chaque ligne (email via Pydantic `EmailStr`, username requis), ignore proprement les doublons (dans le fichier ou déjà en base — **même contournement que le fix `/register`** : pas de filtre `deleted_at`, la contrainte UNIQUE s'applique à toutes les lignes) sans faire échouer tout l'import. Chaque compte créé reçoit un mot de passe aléatoire jamais communiqué (même pattern que les comptes invités) et un email d'invitation réutilisant le mécanisme de réinitialisation de mot de passe existant (nouveau `send_account_invitation_email`, même token/lien que `/reset-password`, texte adapté à un premier accès). Plafonné à `MAX_CSV_IMPORT_ROWS` (défaut 500). Nouveau bouton "Importer un CSV" dans l'espace admin (`frontend/components/dashboard/CsvImportDialog.tsx`), avec résumé créés/ignorés + raison par ligne. Bump `2.10.0` → `2.11.0` (nouvel endpoint rétrocompatible). Vérifié réellement : `ruff check .` propre, 151 tests pytest collectés sans erreur, 10 nouveaux tests backend (`test_csv_user_import.py` : permissions admin-only, création + comptage, colonnes manquantes, email invalide ignoré, doublon fichier, email déjà utilisé actif et soft-deleted, limite de lignes) + sanity-check autonome du parsing/validation CSV hors DB, `tsc --noEmit`/`eslint`/`npm run build`/82 tests Jest (dont les 3 nouveaux `CsvImportDialog.test.tsx`) tous réels. **Non exécuté en local contre un vrai Postgres** (même limite que le reste de la session) — à confirmer par la CI. Non testé manuellement dans le navigateur pour la même raison (pas de Postgres local pour se connecter en admin)
- [x] **Logo intégré** — `frontend/public/logo_smartticket.png` (fourni par l'utilisateur, 1024×1024, fond transparent) remplace le carré violet placeholder "S" partout où il apparaissait : header + footer de la landing page, header des 5 pages d'authentification (login/sign-up/forgot-password/reset-password/verify-email — header ET badge de titre à 48px pour les 4 premières), header des pages légales. Rendu via `next/image` (dimensions explicites par occurrence : 24/28/32/48px selon le contexte), sans conteneur `bg-indigo-600` autour puisque le logo a déjà sa propre identité visuelle (halo bleu). `favicon.ico` volontairement non touché (déjà un vrai favicon multi-résolution antérieur à cette session, pas un placeholder Next.js par défaut — à changer seulement sur demande explicite). Vérifié réellement : `tsc --noEmit`, `eslint`, `npm run build` (build production complet, toutes les routes générées) et 85 tests Jest passent tous. **Non vérifié visuellement dans un navigateur** (pas d'outil de capture d'écran/navigateur disponible dans cet environnement) — à confirmer visuellement par l'utilisateur. Pas de bump de version (changement visuel uniquement, aucun contrat d'API)
- [x] **Coupe-circuit d'abonnement (modèle "flotte d'instances")** — l'opérateur héberge et paye Render pour chaque instance client ; il fallait un moyen de couper l'accès si un client ne paye plus, sans dépendre du compte admin de ce client (justement contrôlé par la partie qui pourrait ne plus payer). Portée volontairement réduite à ce coupe-circuit seul (pas de Stripe ni de panel central pour l'instant — décision explicite de l'utilisateur, prématuré à 0-1 client). Nouveau `GET`/`PUT /v1/instance/subscription-status`, protégés par un secret `VENDOR_KEY` distinct du rôle admin classique (nouveau, `routers/instance.py`, sur le modèle de `X-Setup-Key`/`ADMIN_SETUP_KEY` pour `/setup-admin`). Nouveau modèle `InstanceSubscription` (ligne unique id=1, sans `tenant_id` — cette table scope déjà l'instance entière). Nouveau middleware `enforce_subscription_status` dans `main.py` : bloque toute requête `/v1/*` avec `402 Payment Required` tant que le statut est `suspended`, sauf le health check `GET /` (sinon Render tue le conteneur) et l'endpoint de statut lui-même (sinon l'opérateur ne pourrait plus jamais réactiver). **Fail-open explicite** : `VENDOR_KEY` non défini ou erreur DB transitoire → aucun blocage (un blocage global accidentel vaut bien pire qu'un contrôle manqué ponctuellement). `backend/db/init-db.sql` resynchronisé avec le nouveau modèle. Bump `2.11.0` → `2.12.0` (nouveaux endpoints rétrocompatibles, comportement par défaut inchangé). Vérifié réellement : `ruff check .` propre, 162 tests pytest collectés sans erreur, 11 nouveaux tests (`test_subscription_kill_switch.py` : protection `VENDOR_KEY` sur GET/PUT, statut par défaut actif, suspension/réactivation, statut invalide rejeté, health check et endpoint de statut toujours joignables même suspendu, endpoints normaux bloqués `402` puis restaurés après réactivation). `conftest.py` mis à jour (`VENDOR_KEY` de test + `instance_subscription` ajoutée au `TRUNCATE` inter-tests, sans quoi une suspension dans un test contaminerait les suivants). **Non exécuté en local contre un vrai Postgres** (même limite que le reste de la session) — à confirmer par la CI
- [x] **Bug trouvé en prod juste après le déploiement du logo** : `/logo_smartticket.png` renvoyait un `307` vers `/login` au lieu de servir l'image — même bug de fond que `/verify-email` plus tôt dans la session (`frontend/proxy.ts` redirige tout chemin absent de sa liste `isPublicPath` vers `/login` pour un visiteur non authentifié), mais cette fois sur un **fichier statique** plutôt qu'une page, donc pas couvert par la liste de routes existante. Corrigé plus largement cette fois pour éviter de recroiser ce bug à chaque nouveau fichier ajouté dans `public/` : `isPublicPath` reconnaît désormais tout chemin se terminant par une extension de fichier (regex `/\.[a-zA-Z0-9]+$/`) comme public, quel que soit son nom — une vraie page App Router n'a jamais de point dans son dernier segment, donc ce test ne peut pas laisser passer une page par erreur. `favicon.ico` retiré de la liste explicite (désormais couvert par la même règle générique). Nouveau test dans `proxy.test.ts` (4 extensions différentes : `.png`, `.ico`, `.txt`, `.woff2`). Vérifié réellement : `tsc --noEmit`, `eslint`, `npm run build`, 89 tests Jest (dont les 4 nouveaux) passent tous ; confirmé en prod via `curl` que `/logo_smartticket.png` renvoyait bien un `307` avant le fix (root-caused sans accès navigateur, à partir d'une capture d'écran partagée par l'utilisateur montrant l'icône d'image cassée). Pas de bump de version (correctif frontend, aucun contrat d'API changé)
- [x] **Étape A du provisioning (docs/FLEET_PROVISIONING_PLAN.md) : outillage CLI de gestion de la flotte** — nouveau dossier `ops/` (jamais copié dans une image Docker déployée, en dehors des contextes `./backend`/`./frontend`) : `provision_client.py` (crée Postgres + backend + frontend Render pour un nouveau client, idempotent par rejet sur `--slug` déjà connu, `--dry-run`), `update_all_instances.py` (redeploy en masse, `--dry-run`, `--only slug1,slug2` pour un rollout progressif), `delete_client.py` (décommissionne un client, confirmation interactive requise sauf `--yes`), `render_client.py` (wrapper API Render), `db.py` (table `instances`). **Écart assumé par rapport au plan initial** : table `instances` en SQLite plutôt qu'un Postgres dédié — à l'échelle visée (1-5 clients, gestion en CLI + une requête SQL, zéro interface graphique tant que la douleur n'est pas réelle, cf. instruction explicite de l'utilisateur), payer un Postgres managé pour une poignée de lignes est disproportionné. **Amorçage admin encore temporaire** : la page `/setup?key=...` sans mot de passe transmis (prévue Phase 2 du plan) n'est pas construite — hors scope explicite de cette étape (uniquement les 3 scripts + la table) — `provision_client.py` affiche un mot de passe temporaire une seule fois en console en attendant. Bug réel trouvé et corrigé en testant : emoji `⚠️` dans les docstrings faisait planter `--help` sur une console Windows en cp1252 (`UnicodeEncodeError`) — remplacé par du texte brut + `sys.stdout.reconfigure(encoding="utf-8")` ajouté en tête de chaque script pour que les accents s'affichent correctement quel que soit le codepage par défaut du terminal. **Vérifié réellement (sans toucher à un vrai compte Render — aucun token disponible ni action destructrice prise unilatéralement)** : `ruff check .` propre, les 3 scripts s'importent et leur `--help` s'affiche correctement, `provision_client.py --dry-run` produit le résumé attendu, rejet du plan Postgres `free`, rejet d'un `--slug` déjà présent en base, `update_all_instances.py --dry-run` sur flotte vide, `delete_client.py` sur un slug inconnu, cycle complet insert/list/filtre SQLite testé directement. **Non validé contre un vrai compte Render** (Phase 0 du plan non cochée : aucun appel réel n'a été fait, la forme exacte des payloads Render doit être confirmée avant tout client réel) — `ops/README.md` et les docstrings de chaque script le rappellent explicitement. Pas de bump de version (outillage vendeur, ne fait pas partie du produit déployé)

### Fonctionnel / produit (2026-07-11)
- [x] Landing page repositionnée pour les entreprises — section "Construit sur des technologies modernes" (stack technique exposée publiquement, sans valeur pour un acheteur B2B) retirée de `frontend/app/page.tsx` et remplacée par une section "Entreprises" (sécurité de niveau entreprise, scalabilité, gestion multi-équipes/multi-rôles, accompagnement dédié), avec son lien de nav mis à jour (`#tech` → `#entreprises`). Vérifié réellement : `tsc --noEmit`, `eslint` et un lancement local du serveur de dev (`200 OK` sur `/`) passent tous. Pas de bump de version (page statique, aucun contrat d'API changé)
- [x] Bouton "Se connecter" de la section CTA finale de la landing page (`frontend/app/page.tsx`) entièrement blanc et illisible — le variant `outline` du composant `Button` applique par défaut `bg-background` (blanc), jamais surchargé par les classes custom (`border-white/40 text-white hover:bg-white/10`), qui n'ajustaient que la bordure/le texte/le survol. Résultat : un bouton blanc sur fond blanc avec un texte blanc invisible dessus. Corrigé en ajoutant `bg-transparent`. Vérifié réellement : `tsc --noEmit`, `eslint` propres. Pas de bump de version (correctif CSS, aucun contrat d'API changé)
- [x] Hiérarchie par date des conversations dans le tableau de bord utilisateur — la liste de `UserDashboard.tsx` était plate (juste triée par date, sans repères visuels). Nouveau `frontend/components/dashboard/dateGrouping.ts` (`groupByDate`/`getDateGroupLabel`, sans dépendance externe) : regroupe les sessions déjà triées par date décroissante (l'API `GET /v1/sessions` trie déjà côté serveur) sous des en-têtes "Aujourd'hui" / "Hier" / "7 derniers jours" / "30 derniers jours" / puis "Mois AAAA" pour les plus anciennes, sans re-trier ni modifier la pagination existante (le regroupement s'applique à la page courante de 10 éléments). Non appliqué au mode recherche (résultats déjà filtrés par pertinence, pas par date). Vérifié réellement : `tsc --noEmit`, `eslint` propres, nouveau `__tests__/dateGrouping.test.ts` (5 tests : libellés Aujourd'hui/Hier/7j/30j/mois, regroupement sans réordonnancement), 101 tests Jest au total, `npm run build` passe. Pas de bump de version (UI uniquement, aucun contrat d'API changé)
- [x] Tableau de bord utilisateur et gestion des conversations améliorés (`UserDashboard.tsx`) — trois changements : (1) **filtre par statut** (Toutes/Ouvertes/Transférées/Clôturées, avec compteur par filtre), pour retrouver rapidement des conversations dans une liste qui grossit ; (2) **bug corrigé** : le compteur "Conversations ouvertes" comptait `total - clôturées`, incluant donc à tort les sessions transférées dans les "ouvertes" — séparé en un vrai calcul par statut (`sessionStatusKey`) ; (3) **espace mort de la grille comblé** : la carte de liste n'occupait que 5 colonnes sur 7 (`lg:grid-cols-7` / `lg:col-span-5`), laissant 2 colonnes vides à large écran — ajout d'une carte "Répartition" (barres de proportion ouvertes/transférées/clôturées) qui réutilise les compteurs déjà calculés. Filtre non appliqué en mode recherche (résultats déjà filtrés par pertinence). Vérifié réellement : `tsc --noEmit`, `eslint` propres, 2 nouveaux tests Jest (filtre + panneau de répartition, état vide spécifique au filtre actif), 103 tests Jest au total, `npm run build` passe. **Non testé manuellement dans un navigateur** (pas de Postgres local pour se connecter, même limite que le reste de la session). Pas de bump de version (UI uniquement, aucun contrat d'API changé)
- [x] Hiérarchie par date repliable dans la sidebar de discussions (`app-sidebar.tsx`) — demandée par l'utilisateur après une capture d'écran montrant la liste "Discussions" toujours plate, contrairement au tableau de bord principal qui a déjà `groupByDate`. Réutilise le même helper (`frontend/components/dashboard/dateGrouping.ts`) : ajout de `date_creation` à l'interface `Conversation` et au mapping de `fetchConversations` (absent jusqu'ici, la sidebar ne récupérait que id/titre/statut), puis regroupement sous les mêmes en-têtes "Aujourd'hui" / "Hier" / "7 derniers jours" / "30 derniers jours" / "Mois AAAA". **Nouveau par rapport aux autres listes déjà groupées** : chaque en-tête de groupe est cliquable (icône chevron qui pivote) et replie/déplie ses conversations, comme demandé explicitement ("comme un menu déroulant") — état `collapsedGroups` (Set des libellés repliés), tous les groupes dépliés par défaut. Vérifié réellement : `tsc --noEmit`, `eslint` propres, nouveau `__tests__/AppSidebar.test.tsx` (2 tests : regroupement par période, repli/dépli au clic sur l'en-tête), 105 tests Jest au total, `npm run build` passe. **Non testé manuellement dans un navigateur** (pas de Postgres local pour se connecter, même limite que le reste de la session). Pas de bump de version (UI uniquement, aucun contrat d'API changé)
- [x] **Bug remonté par l'utilisateur via une capture d'écran de prod** : la carte "Répartition" ajoutée juste au-dessus (point 3) répétait les mêmes chiffres (ouvertes/transférées/clôturées/total) déjà visibles dans les 4 cartes de stats en haut de page *et* dans les compteurs des onglets de filtre — trois affichages redondants de la même information sur un seul écran. Carte "Répartition" retirée de `UserDashboard.tsx`, la carte de liste repasse en pleine largeur (grille `lg:grid-cols-7` simplifiée). Les onglets de filtre sont conservés (ils ont une fonction, filtrer, contrairement à la carte retirée qui n'était que décorative). Vérifié réellement : `tsc --noEmit`, `eslint` propres, test Jest ajusté (retrait des assertions sur la carte supprimée), 103 tests Jest au total, `npm run build` passe. Pas de bump de version (UI uniquement, aucun contrat d'API changé)
- [x] Tri des conversations par jour/semaine/mois étendu au SAV — la hiérarchie par date (`groupByDate`, déjà en place côté `UserDashboard.tsx`) était absente de `SavDashboard.tsx` : la file d'attente des tickets transférés était une liste plate. Réutilise le même helper (aucune nouvelle logique de regroupement) pour afficher les mêmes en-têtes "Aujourd'hui" / "Hier" / "7 derniers jours" / "30 derniers jours" / "Mois AAAA" au-dessus des groupes de tickets dans la sidebar de file d'attente ; les séparateurs `divide-y` (qui ne s'appliquaient plus qu'entre groupes une fois la liste imbriquée) remplacés par une bordure par ticket pour conserver le même rendu visuel qu'avant. Vérifié réellement : `tsc --noEmit`, `eslint` propres, nouveau test Jest (`groups the queue by date period`, tickets d'aujourd'hui et de janvier 2026 sous deux en-têtes distincts), 104 tests Jest au total, `npm run build` passe. **Non testé manuellement dans un navigateur** (pas de Postgres local pour se connecter, même limite que le reste de la session). Pas de bump de version (UI uniquement, aucun contrat d'API changé)
- [x] Gestion de l'escalade IA ↔ SAV : reprise de main côté client — la question ouverte de l'item ROADMAP ("géré par le client ou le SAV ?") était déjà à moitié répondue : un agent SAV pouvait déjà rendre la main à l'IA (`POST /v1/sessions/{id}/resolve`, bouton "Remettre à l'IA" dans `SavDashboard.tsx`), mais le client n'avait aucun moyen de le faire lui-même une fois sa question traitée. **Décision** : les deux — `resolve_session` (`backend/routers/sessions.py`) accepte désormais aussi le propriétaire de la session (`session.id_utilisateur == user.id`), en plus de SAV/admin, avec une garde spécifique au client : il ne peut reprendre la main **qu'une fois qu'un agent SAV a effectivement répondu** (vérifié par un `SELECT` sur `chat_messages` filtré `type_envoyeur == "sav"`) — sinon il pourrait court-circuiter le transfert qu'il vient de demander en repassant à l'IA avant même d'avoir été aidé. Le SAV, lui, reste libre de résoudre à tout moment (comportement inchangé). Message système généré différencié selon l'auteur ("par l'agent SAV" / "par le client"). Frontend : nouveau bouton "Reprendre avec l'IA" dans la bannière ambre de `frontend/app/(chat)/ai-assistant/[id]/page.tsx`, affiché uniquement quand `isTransferred && hasSavReply` (nouvel état dérivé de `has_sav_reply`, déjà renvoyé par `GET /v1/sessions` mais jusque-là ignoré sur cette page). Pas de bump de version (durcissement d'un endpoint existant, contrat de réponse inchangé). Vérifié réellement : `ruff check .` propre, 187 tests pytest collectés sans erreur (6 nouveaux dans `backend/tests/test_session_resolve.py` : refus avant réponse SAV, autorisation après réponse SAV, SAV toujours libre, tiers refusé, session non transférée refusée, 401 sans auth) — **non exécutés contre un vrai Postgres** (identifiants du Postgres local indisponibles dans cet environnement, même limite que le reste de la session). Côté frontend, entièrement vérifié réellement : `tsc --noEmit`, `eslint`, nouveau `__tests__/AiAssistantResolve.test.tsx` (2 tests, avec mock de `streamdown` — package ESM-only jamais testé jusqu'ici, aucun test n'important cette page avant), 108 tests Jest au total, `npm run build` passe
- [x] **Export RGPD (`GET /v1/me/export`) enrichi d'un sommaire** — le PDF listait déjà le profil puis le détail brut de chaque conversation/message, sans aucune vue d'ensemble. Nouvelle section "Résumé" dans `backend/pdf_export.py::build_user_data_export_pdf` (entre "Profil" et le détail) : nombre total de conversations, nombre de messages échangés, répartition par statut (ouvertes/transférées/clôturées), et période couverte (première → dernière conversation). **Bug latent corrigé au passage** : le docstring de la fonction documentait déjà un champ `date_creation` par session dans `sessions_with_messages`, mais `routers/auth.py::export_my_data` ne le peuplait jamais — ajouté (`s.date_creation.strftime(...)`), nécessaire pour calculer la période du sommaire. Pas de bump de version (contenu du PDF enrichi, endpoint et son contrat inchangés). Vérifié réellement : `ruff check .` propre, 184 tests pytest collectés sans erreur, nouveau `backend/tests/test_user_data_export.py` (3 tests : 401 sans auth, sommaire correct avec sessions ouverte+clôturée, cas sans conversation) — **non exécutés contre un vrai Postgres** (même limite que le reste de la session), mais le générateur PDF lui-même vérifié réellement hors DB (script autonome avec données réalistes + cas vide, texte extrait du PDF produit via `pypdf` et relu pour confirmer le rendu exact du sommaire)
- [x] **Suggestions de démarrage génériques sur une nouvelle conversation** — les 4 cartes proposées sur `frontend/app/(chat)/ai-assistant/[id]/page.tsx` (page vide) étaient codées en dur sur un scénario Stripe (« Remboursement Stripe », « Erreur carte ou CVV », « Délai de litige ») alors que SmartTicket est un produit multi-tenant où chaque client déployé a un domaine métier totalement différent (RH, gendarmerie, projets internes — vu dans des captures précédentes) : ces suggestions n'avaient de sens pour quasiment aucun client réel. Remplacées par 4 suggestions génériques (suivi de commande, retour/remboursement, contact agent humain, gestion du compte), alignées sur l'exemple déjà utilisé sur la landing page (« Comment puis-je suivre ma commande ? »). **Option écartée** : générer dynamiquement les suggestions depuis la base de connaissances réelle du tenant — plus pertinent en théorie, mais nécessite soit un appel LLM par suggestion (coût/latence à chaque chargement de page), soit d'exposer des extraits de sources KB à un rôle `user` (`GET /knowledge-base/sources` est admin/sav-only, durci intentionnellement plus tôt dans la session) ; jugé disproportionné pour ce correctif, à reconsidérer séparément si le besoin se confirme. Pas de bump de version (contenu statique, aucun contrat d'API changé). Vérifié réellement : `tsc --noEmit`, `eslint` propres, nouveau `__tests__/AiAssistantSuggestions.test.tsx` (nouvelles suggestions visibles, aucune trace de "Stripe"/"CVV"), 109 tests Jest au total, `npm run build` passe
- [x] **Dark thème** — étant donné l'ampleur (refonte transversale du frontend), l'utilisateur a choisi de traiter ce chantier avant celui des langues (les deux items de l'"À faire" ayant été présentés ensemble). Nouveau `frontend/components/theme-provider.tsx` (wrapper client autour de `next-themes`, `attribute="class"` + `defaultTheme="system"`) branché dans `app/layout.tsx` ; les tokens `.dark` de shadcn existaient déjà dans `globals.css` mais n'avaient jamais été activés. Sélecteur Clair/Sombre/Système dans `frontend/components/ThemeToggle.tsx` (nouvelle carte "Apparence" dans Paramètres), plus un raccourci icône Soleil/Lune dans le pied de la sidebar (`app-sidebar.tsx`) pour basculer rapidement entre clair et sombre. **Audit des couleurs codées en dur** sur les pages/dashboards de l'app authentifiée (`UserDashboard`, `SavDashboard`, `AdminDashboard`, `SupervisorDashboard`, la page de chat `ai-assistant/[id]`, `knowledge-base`, `monitoring`, `settings`, `GuestClaimBanner`, `ui/progress.tsx`) : remplacement systématique de `bg-white`/`text-slate-*`/`border-slate-*`/`bg-slate-*` par les tokens sémantiques (`bg-card`, `text-foreground`, `text-muted-foreground`, `border-border`, `bg-muted`) qui basculent automatiquement avec `.dark`. **Portée volontairement limitée** : les badges de statut colorés (emerald/amber/slate-100) et bannières d'alerte teintées sont laissés tels quels (accents délibérément vifs, lisibles dans les deux thèmes, pattern déjà utilisé partout dans l'app) — seules les vraies surfaces de page (fond, texte principal, bordures) ont été migrées. **Bug trouvé et corrigé en testant réellement avec Playwright** (capture d'écran de `/login` en préférence sombre) : les pages publiques (landing, connexion/inscription, mentions légales, chat invité) utilisent des dégradés semi-transparents (`via-indigo-50/40`) qui laissaient transparaître le fond sombre du `<body>` désormais actif globalement, produisant un rendu grisâtre non voulu sur des pages volontairement laissées "toujours claires". Corrigé par une nouvelle classe `.force-light` dans `globals.css` (redéclare les valeurs `:root`, plus `color-scheme: light`), appliquée aux layouts `(auth)`, `(legal)`, à la landing page et à `app/chat/page.tsx`, combinée à un `bg-background` explicite pour peindre une surface opaque sous les dégradés. Pas de bump de version (UI uniquement, aucun contrat d'API changé). Vérifié réellement : `tsc --noEmit`, `eslint` propres (dont un vrai avertissement `react-hooks/set-state-in-effect` sur le pattern "mounted" recommandé par `next-themes`, supprimé explicitement avec commentaire justificatif), 109 tests Jest, `npm run build` passe. **Testé visuellement pour de vrai** (contrairement à la plupart des entrées précédentes de cette session) : serveur de dev lancé, Chromium Playwright installé à la volée (`npm install --no-save`, aucune trace dans `package.json`/lock), captures d'écran de `/login` en clair et en préférence sombre (bug de bleed-through détecté puis corrigé), `/` et `/mentions-legales` confirmées toujours claires après le correctif. **Non testé visuellement** : les dashboards authentifiés eux-mêmes (`UserDashboard`, `SavDashboard`, etc.) — pas d'identifiants pour le Postgres local de cet environnement, même limite que le reste de la session ; la migration de couleurs y est vérifiée uniquement par grep exhaustif (zéro couleur codée en dur restante) + compilation/tests, pas par capture d'écran
- [x] **Anglais (i18n) — infra + 2 pages en modèle** — deux décisions structurantes tranchées avec l'utilisateur avant de commencer (chantier bien plus vaste que le dark thème : chaque texte de l'app est actuellement une chaîne française codée en dur) : (1) **pas de préfixe d'URL** (`/fr/...`/`/en/...`) — la langue est une préférence stockée en `localStorage`, aucune route existante déplacée ; (2) **périmètre de cette session limité à l'infra + 2 pages modèles** (landing page + connexion), le reste de l'app reste en français pour l'instant, à traduire ensuite selon le même patron. **Décision technique** : pas de `next-intl` malgré son statut d'outil standard Next.js — sa lecture de locale nécessite soit un préfixe d'URL, soit un cookie lu côté serveur dans `i18n/request.ts`, ce qui aurait rendu tout le layout racine (et donc toutes les pages, y compris celles actuellement statiques) dynamique côté rendu ; disproportionné pour 2 pages traduites. Solution maison à la place : `frontend/lib/i18n/translations.ts` (dictionnaires `fr`/`en` typés — `en: typeof fr` force TypeScript à garder les deux structures strictement identiques, garde-fou de complétude), `frontend/lib/i18n/LocaleContext.tsx` (contexte React 100% client, préférence lue depuis `localStorage` après montage), `frontend/components/LanguageToggle.tsx` (sélecteur Français/English, même style que `ThemeToggle`). Landing page (`app/page.tsx`, passée en composant client) et page de connexion entièrement traduites, sélecteur de langue ajouté dans les deux en-têtes. **Compromis assumé** : léger flash "français puis langue choisie" au premier rendu (pas de lecture SSR), jugé acceptable pour garder `/` et `/login` en rendu statique (confirmé inchangé dans la sortie de `npm run build` : toujours `○ Static`) plutôt que de les faire basculer en dynamique pour cette seule fonctionnalité. Pas de bump de version (UI uniquement, aucun contrat d'API changé). Vérifié réellement : `tsc --noEmit`, `eslint` propres, `__tests__/LoginPage.test.tsx` adapté (wrapper `LocaleProvider`), nouveau `__tests__/LanguageToggle.test.tsx` (bascule FR→EN sur la landing page, persistance en `localStorage`), 111 tests Jest au total, `npm run build` passe (`/` et `/login` toujours statiques). **Testé visuellement pour de vrai** : serveur de dev + Chromium Playwright (même méthode que le dark thème), captures d'écran de la landing page en français et en anglais après clic sur le sélecteur, et de `/login` confirmant que le choix de langue persiste bien en changeant de page
- [x] **Anglais (i18n) — extension à la sidebar et au tableau de bord utilisateur** — remonté par l'utilisateur avec une capture d'écran de l'app connectée (sidebar + `UserDashboard`) : la landing page et la connexion étaient traduites, mais l'app elle-même (l'essentiel de l'usage réel, contrairement aux pages d'entrée vues une seule fois) restait figée en français, sans même de sélecteur de langue visible une fois connecté. Nouvelles clés `common.dateGroups`, `sidebar.*` et `dashboard.*` dans `translations.ts`. **Changement transverse nécessaire** : `dateGrouping.ts` (`getDateGroupLabel`/`groupByDate`) prenait ses libellés ("Aujourd'hui", "Hier"...) en dur — étendu avec des paramètres `labels`/`locale` optionnels à valeur par défaut française (`DEFAULT_DATE_GROUP_LABELS`), pour que `SavDashboard.tsx` (pas encore dans le périmètre traduit) continue de fonctionner sans aucune modification. `app-sidebar.tsx` et `UserDashboard.tsx` intégralement traduits (menu, discussions, filtres de statut, cartes de stats, badges, pagination, messages d'erreur), `LanguageToggle` ajouté dans le pied de la sidebar (jusqu'ici seul le bouton de thème clair/sombre y était présent). Pas de bump de version (UI uniquement, aucun contrat d'API changé). Vérifié réellement : `tsc --noEmit`, `eslint` propres, `__tests__/AppSidebar.test.tsx` et `__tests__/UserDashboard.test.tsx` adaptés (wrapper `LocaleProvider`), nouveau test dans `LanguageToggle.test.tsx` qui rend la sidebar et le tableau de bord **ensemble** sous un même `LocaleProvider` et vérifie que cliquer sur le sélecteur dans la sidebar traduit bien aussi le dashboard (preuve que la synchronisation inter-composants fonctionne, pas juste chaque composant isolément), 112 tests Jest au total, `npm run build` passe. **Non testé visuellement dans un vrai navigateur** (contrairement à l'incrément précédent) : le tableau de bord authentifié n'est accessible qu'après connexion, qui nécessite le Postgres local indisponible dans cet environnement — vérifié uniquement via le test d'intégration Jest ci-dessus (rendu réel des deux composants ensemble, pas de mock du contenu traduit)

- [x] **Anglais (i18n) — extension à tout le reste de l'app** — remonté par l'utilisateur avec insistance après le point précédent ("bah je te l'avais demandé !") : la sidebar et le tableau de bord utilisateur étaient traduits, mais tous les autres écrans authentifiés (SAV, admin, superviseur, base de connaissances, monitoring, paramètres, chat IA) et toutes les pages publiques hors landing/connexion (inscription, mots de passe oublié/réinitialisation, vérification email, configuration admin, mentions légales, politique de confidentialité, CGV) restaient intégralement en français codé en dur. Traité en un seul passage cette fois, sans redécouper le périmètre. Nouvelles clés `common.reasons`, `chat.*`, `sav.*`, `supervisor.*`, `admin.*`, `settings.*`, `monitoring.*`, `knowledgeBase.*`, `signUp.*`, `forgotPassword.*`, `resetPassword.*`, `verifyEmail.*`, `setup.*`, `legal.*` dans `translations.ts` (fr/en, plus de 300 clés ajoutées). `SavDashboard.tsx`, `SupervisorDashboard.tsx`, `AdminDashboard.tsx`, `settings/page.tsx` (+ nouvelle carte "Langue" avec `LanguageToggle`, jusque-là absente hors sidebar), `monitoring/page.tsx`, `knowledge-base/page.tsx`, `ai-assistant/[id]/page.tsx`, et les 5 pages `(auth)` intégralement traduits. **Cas particulier pages légales** : `mentions-legales`, `politique-confidentialite` et `cgv` sont des Server Components (`export const metadata`), incompatibles avec `useLocale()` (contexte client-only, choix assumé dès l'infra pour garder le rendu statique) — extraction du contenu de chaque page dans un composant client dédié (`MentionsLegalesContent.tsx` etc.) importé par un `page.tsx` resté Server Component pour conserver le `metadata` ; `(legal)/layout.tsx` passé en client component au passage pour y ajouter le `LanguageToggle` (absent jusqu'ici). Contenu des pages légales traduit tel quel, y compris les marqueurs `[À COMPLÉTER]` (traduits en `[TO BE COMPLETED]`) — ce sont des brouillons non validés juridiquement (déjà noté dans une entrée précédente), aucune tentative de "corriger" le fond en le traduisant. **Un agent en arrière-plan lancé pour paralléliser les pages auth/légales a échoué** (limite de session API atteinte) : ce lot a finalement été traité directement dans cette conversation plutôt qu'en sous-tâche. Pas de bump de version (UI uniquement, aucun contrat d'API changé). Vérifié réellement : `tsc --noEmit` et `eslint .` propres sur l'ensemble du frontend (seuls les 2 avertissements `react-hooks/exhaustive-deps` pré-existants sur `AdminDashboard`/`SupervisorDashboard` subsistent, confirmés antérieurs à cette session via `git stash`), tous les fichiers de test affectés (`SavDashboard`, `SupervisorDashboard`, `AdminDashboard`, `SettingsPage`, `SignUpPage`, `ForgotPasswordPage`, `ResetPasswordPage`, `VerifyEmailPage`, `SetupPage`, `AiAssistantSuggestions`, `AiAssistantResolve`) adaptés avec le wrapper `LocaleProvider`, 112 tests Jest toujours au total et tous verts, `npm run build` passe avec toutes les pages concernées toujours generées statiquement (`○ Static`, y compris les 8 pages `(auth)`/`(legal)` — confirmation que l'extension n'a pas cassé le rendu statique). **Non testé visuellement dans un vrai navigateur** (même limite que les incréments i18n précédents : pas de Postgres local pour se connecter et voir les dashboards authentifiés réels) ; les messages d'alerte IA générés côté backend (`monitoring/page.tsx`, champ `alert.message`/`alert.recommendation` venant de l'API) restent en français quel que soit la langue choisie côté frontend — hors périmètre de ce chantier purement frontend, à traiter si besoin lors d'un futur passage d'i18n côté backend

### Pages légales (2026-08-25)
- [x] **Brouillons des 3 pages légales + DPA étoffés** (clauses génériques manquantes identifiées lors d'un état des lieux dédié) — mentions légales : ajout RCS/capital social/n° TVA intracommunautaire (conditionnels selon la forme juridique, en `[À COMPLÉTER]`) et coordonnées complètes de l'hébergeur pour conformité LCEN (dénomination + adresse de Render Services, Inc. vérifiées par recherche web — 525 Brannan Street, Suite 300, San Francisco — téléphone laissé en `[À COMPLÉTER]`, non retrouvé avec une confiance suffisante pour figurer dans un document légal) ; politique de confidentialité : durée de conservation des données de facturation (10 ans, obligation comptable française) et droit à la limitation du traitement (art. 18 RGPD, absent jusqu'ici) ; CGV : pénalités de retard + indemnité forfaitaire de recouvrement de 40 € désormais rédigées en clair (art. L441-10 Code de commerce, obligatoire en B2B — auparavant un simple placeholder `[À COMPLÉTER]`), nouvel article "Force majeure", nouvel article "Modification des CGV", et un plafond de responsabilité (12 mois de montants payés) explicitement marqué **`[À VALIDER PAR UN AVOCAT]`** — proposé à titre indicatif, pas validé juridiquement. Articles CGV renumérotés en conséquence (1 à 10). Tous les champs d'identité de l'entité (raison sociale, forme juridique, SIREN, adresse, etc.) restent des `[À COMPLÉTER]` explicites, volontairement jamais inventés — l'entité n'est pas encore créée. **Décision explicite de l'utilisateur : le `DPA.md` et les clauses "protection des données"/CGV n'ont volontairement PAS été retouchés sur le fond** — une décision d'architecture reste en suspens (Tiqia héberge-t-il la base du Client, ou le Client apporte-t-il la sienne ?), qui changerait la qualification RGPD de Tiqia (sous-traitant vs coresponsable) et donc tout le contenu du DPA ; un avertissement explicite a été ajouté en tête de `DPA.md` pour signaler cette dépendance plutôt que de figer un contenu qui devra de toute façon être repris. FR/EN maintenus strictement synchronisés (`en: typeof fr` dans `translations.ts` — parité de clés vérifiée par diff explicite, pas seulement par relecture). Pas de bump de version (contenu de pages statiques + document annexe, aucun contrat d'API changé). **Non vérifié par `tsc`/`eslint`/`npm run build`/tests Jest** : Node.js n'est pas installé dans cet environnement (aucun `node`/`npm`/`npx` en PATH, contrairement aux sessions précédentes de ce même journal) — vérifié uniquement par relecture manuelle complète des 3 composants `.tsx` modifiés et par un diff explicite des clés FR/EN de `translations.ts` (identiques). **Rappel obligatoire, à ne pas perdre de vue** : ces documents (les 3 pages + `DPA.md`) restent une base non validée juridiquement — relecture par un avocat spécialisé RGPD/SaaS obligatoire avant toute première signature client, cf. item "Relecture juridique" dans "À faire" ci-dessous.
- [x] **Pages légales déplacées de `frontend/` vers `landing/`** — décision explicite de l'utilisateur : les 3 pages (mentions légales, politique de confidentialité, CGV) identifient l'entité Tiqia elle-même, pas une instance client, donc n'ont pas leur place dans `frontend/` (déployé une fois par client). Recréées à l'identique dans `landing/app/(legal)/` (même structure layout + page + composant client, déjà statique des deux côtés — `output: "export"` côté `landing/`, pages déjà générées `○ Static` côté `frontend/`, donc portage direct sans changement de pattern). Contenu juridique (clés `legal.*`, FR+EN) déplacé de `frontend/lib/i18n/translations.ts` vers `landing/lib/i18n/translations.ts` — parité stricte des clés FR/EN reconfirmée par diff après coup des deux côtés. Footer de `landing/app/page.tsx` corrigé : les 3 liens pointaient vers `${APP_URL}/...` (une instance frontend externe, fragile — `README.md`/commentaire de `brand.ts` mis à jour en cohérence) ; ils pointent désormais vers les routes locales (`/mentions-legales`, etc.), sur le même domaine. `APP_URL` conservée (toujours utilisée par les CTA de connexion). Côté `frontend/` : dossier `app/(legal)/` supprimé, clés `legal.*` et `landing.footer.legal/privacy/cgv` (devenues orphelines) retirées de `translations.ts`, lien du footer de la landing d'instance (`app/page.tsx`) retiré, `proxy.ts`/`proxy.test.ts` nettoyés des 3 chemins publics disparus. **Décision à noter, prise par défaut plutôt que validée explicitement** : le lien vers la politique de confidentialité dans la case à cocher RGPD de `(auth)/sign-up/page.tsx` a été retiré (texte non-cliquable) plutôt que repointé vers `tiqia.fr` — l'énoncé de la tâche demandait littéralement de "retirer" tout lien cassé, mais un consentement RGPD sans aucun lien vers le document consenti est une régression UX/conformité mineure ; à reconsidérer (lien vers `https://tiqia.fr/politique-confidentialite` ?). Pas de bump de version (pages statiques + contenu i18n, aucun contrat d'API changé). **Non vérifié par `tsc`/`eslint`/`npm run build`/tests Jest sur les deux apps** (même limite que l'entrée précédente : Node.js absent de cet environnement) — vérifié par relecture manuelle complète de tous les fichiers créés/modifiés, diff de parité de clés FR/EN sur les DEUX fichiers `translations.ts` (`landing/` et `frontend/`, avant/après), et grep exhaustif confirmant qu'aucune référence aux routes/composants/clés supprimés ne subsiste dans `frontend/`.
- [x] **Pages 404 personnalisées (`landing/` + `frontend/`)** — aucune des deux apps n'avait de `app/not-found.tsx` (vérifié avant toute modification). Créées sur le même pattern déjà établi pour les pages légales : `not-found.tsx` (Server Component, `metadata` title) + `NotFoundContent.tsx` (Client Component, `useLocale`, bilingue FR/EN via de nouvelles clés `notFound.*` dans les deux `translations.ts`) — permet un `<title>` propre sans sacrifier la traduction. Logo horizontal en en-tête + logo `T` au-dessus du "404", style identique aux pages `(auth)`/`(legal)` déjà existantes (`force-light`, dégradé `from-slate-50 via-indigo-50/40 to-white`, `Button` partagé). `frontend/` propose deux boutons (accueil + connexion), `landing/` un seul (accueil), conformément à la demande. **Compatibilité export statique de `landing/` confirmée** : `output: "export"` génère nativement un `404.html` à partir d'un `app/not-found.tsx` (fonctionnalité standard Next.js, aucun changement de config nécessaire) — aucune API dynamique utilisée dans ces fichiers (pas de `cookies()`/`headers()`), exactement le même profil que les pages déjà exportées avec succès. **Détail découvert en creusant `frontend/proxy.ts` pour ce chantier** : une visite anonyme d'une URL inexistante sur une instance client ne verra quasiment jamais cette page — `proxy.ts` redirige déjà tout chemin non couvert par `isPublicPath` vers `/login` avant même d'atteindre le routeur Next.js (comportement préexistant, non lié à cette tâche, non modifié) ; la page ne s'affichera donc que pour un utilisateur déjà authentifié tombant sur une URL cassée, ou en local/dev. **Correction au passage d'une affirmation erronée que j'avais moi-même écrite dans `ROADMAP.md` le 2026-08-25** (cf. ligne "Séparer le site vitrine de l'instance client" ci-dessus) : j'avais affirmé que `frontend/app/page.tsx` servait "toujours" la landing marketing sur `/` d'une instance client, sans avoir vérifié `proxy.ts`/`deploymentMode.ts` — en réalité déjà résolu depuis le 2026-08-15 (`IS_MARKETING_SITE`), item retiré de "à faire". Pas de bump de version (pages statiques + contenu i18n, aucun contrat d'API changé). **Non vérifié par `tsc`/`eslint`/`npm run build` sur les deux apps** (Node.js toujours absent de cet environnement) — vérifié par relecture manuelle complète des 4 fichiers créés, équilibre des accolades/parenthèses, et diff de parité de clés FR/EN sur les deux `translations.ts` après ajout de `notFound.*`.

### Rebrand couleurs premium — tokens de marque nommés (2026-08-26)
- [x] **Palette "premium confiance" (marine + bleu vif + anthracite) appliquée via 4 tokens `oklch()` centralisés**, remplaçant ~298 usages d'indigo/violet/blue Tailwind codés en dur sur 21 fichiers (`frontend/` + `landing/`). Palette validée en amont via un aperçu visuel dédié (échantillonnage réel des couleurs du logo — `logo-T.png`/`logo-app-noir.png` — plutôt qu'inventées, calcul `oklch()` et vérification de contraste WCAG par script Python, Node absent de l'environnement). Tokens ajoutés dans `@theme` des deux `globals.css` : `--color-brand` (#1E4DE6, quasi identique à l'ancien `--primary` — H 264,4° vs 265,0°, la teinte du site était déjà quasi la bonne), `--color-brand-dark` (#0A1832, marine), `--color-brand-accent` (#28AAF0, bleu ciel), `--color-anthracite` (#1A1F2E, remplace le neutre `slate` par défaut pour `--foreground`/`--border`/`--muted`/etc.). Tokens sémantiques existants (`--primary`, `--ring`, `--sidebar-primary`, `--secondary`, `--muted`, `--accent`, `--border`) retintés en clair ET en sombre (`.dark`, `frontend/` uniquement — `landing/` reste mono-thème clair, décision produit assumée). `--background`/`--card`/`--popover`/`--sidebar` sombres retintés en marine désaturé (#12172A/#1B2138) plutôt qu'un gris neutre pur — la dominante marine s'applique aussi au thème sombre lui-même, pas seulement aux accents.
- [x] **Règle stricte respectée par construction, pas seulement vérifiée** : `brand-accent` (#28AAF0) échoue le contraste AA en texte sur fond clair (2.59, calculé) — jamais introduit comme classe utilitaire dans un composant pendant la migration (`grep brand-accent` hors `globals.css` : aucun résultat), réservé aux 3 slots `.dark` où il est soit un ring (pas soumis aux règles de contraste texte), soit un fond pairé avec `brand-dark` en texte (6.81, calculé). Les deux dégradés "hero" (titre landing, "404") utilisent `from-brand-dark to-brand` plutôt que `to-brand-accent` pour cette même raison — un dégradé de texte dont une extrémité échoue le contraste rendrait une partie du texte illisible.
- [x] **Migration mécanique en deux temps** : édition manuelle pour les 6 pages `(auth)` + `page.tsx`/`NotFoundContent.tsx` des deux apps (patterns répétés mais variables selon le contexte — dégradés, badges, focus rings) ; script Python de substitution par regex (`bg|text|border|ring|from|to|via`-`indigo|violet|blue`-`shade` → équivalent `brand` avec opacité proportionnée à la teinte d'origine) pour les fichiers les plus répétitifs (`AdminDashboard.tsx`, `SavDashboard.tsx`, `SupervisorDashboard.tsx`, pages `tickets`/`knowledge-base`/`ai-assistant`, 4 pages légales `landing/`). **Deux bugs introduits par le script et corrigés après coup** : (1) une classe déjà suffixée d'une opacité d'origine (ex. `via-indigo-50/30`) produisait une double opacité (`via-brand/10/30`) — 9 occurrences trouvées et corrigées par grep dédié ; (2) les deux bouts d'un dégradé `from-indigo-600 to-violet-600` convergeaient tous deux vers `brand`, annulant visuellement le dégradé (`from-brand to-brand`) — corrigé en `from-brand-dark to-brand` partout où rencontré. Un troisième cas non couvert par le script (`AdminDashboard.tsx::userColumnClass`, interpolation dynamique `` `bg-${color}-50/60` `` à partir d'un nom de couleur en string) a nécessité de changer le contrat de la fonction (accepter une classe complète plutôt qu'un nom de couleur, `brand` n'ayant pas de rampe `-50` contrairement à `indigo`/`violet`/`blue`/`emerald`).
- [x] **Couleurs fonctionnelles (emerald/red/amber = succès/erreur/alerte) volontairement non touchées**, y compris quand elles suivaient le même pattern de catégorisation que les couleurs de marque migrées (ex. badges de rôle dans `AdminDashboard.tsx` où Utilisateurs/Superviseurs/Admins/Sessions avaient chacun leur teinte — bleu/violet/indigo migrent vers `brand` uniformément, emerald reste emerald). **Compromis assumé** : ces 4 catégories utilisateur partagent désormais la même teinte `brand`, perdant un peu de la distinction visuelle au survol rapide qu'offraient les 3 teintes différentes — jugé acceptable au profit de la centralisation demandée sur un token unique plutôt que d'introduire `brand-accent` dans ces contextes (qui aurait fonctionné visuellement pour les icônes seules, mais plusieurs de ces mêmes teintes servent aussi de texte de badge, où `brand-accent` aurait échoué le contraste).
- [x] **Graphiques Recharts (`monitoring`, `analytics`) rendus adaptatifs au thème** : les couleurs étaient des littéraux hex fixes (grille, axes, tooltip, ligne/barres) invisibles ou peu lisibles en sombre (fond de tooltip blanc, grille claire). Recharts prenant des couleurs littérales sur ses props SVG (pas de classes Tailwind ni de `var()` CSS fiable), une palette `chartColors` dérivée de `useTheme()` (`next-themes`, déjà utilisé ailleurs dans `frontend/`) est calculée côté client et injectée dans chaque prop couleur. La ligne de référence "KB+" (`#10b981`, emerald) reste inchangée dans les deux thèmes — couleur fonctionnelle, hors périmètre. Tokens `--chart-1..5` (définis dans `globals.css` mais jamais consommés par aucun composant, confirmé par `grep`) supprimés plutôt que câblés — aucun renvoi vers eux nulle part dans le code.
- [x] **Neutres retintés vers l'anthracite** (`--foreground`, `--border`, `--muted`, `--secondary`, `--accent`, en clair et en sombre) — chroma OKLCH faible (0.004 à 0.03) pour rester perceptible comme un simple neutre plutôt qu'une couleur affirmée, mais délibérément non nul (neutre "choisi", pas hérité par défaut). `--foreground` clair passe de `oklch(0.145 0 0)` (quasi noir neutre) à `var(--anthracite)` — contraste texte sur blanc resté excellent (16.41, calculé).
Pas de bump de version (CSS + classes utilitaires, aucun contrat d'API changé). **Non vérifié par `tsc`/`eslint`/`npm run build` sur les deux apps** (Node.js absent de cet environnement, même limite que les 3 entrées précédentes) — vérifié par : grep exhaustif confirmant zéro occurrence indigo/violet/blue-shade restante sur tout le repo ; équilibre accolades/parenthèses sur les 27 fichiers `.tsx` touchés ; grep dédié confirmant `brand-accent` absent de tout composant ; relecture manuelle des fichiers les plus modifiés (`AdminDashboard.tsx`, `page.tsx` des deux apps, `monitoring`/`analytics`). **À confirmer avant merge** : build réel des deux apps + contrôle visuel du rendu clair/sombre dans un vrai navigateur.

---

## Stratégie de commercialisation (2026-07-08)

**Décision d'architecture** : flotte d'instances séparées (1 Postgres + 1 backend + 1 frontend par client, déployés via Render) plutôt qu'un vrai multi-tenant partagé. Raisons : (1) une faille d'isolation en multi-tenant partagé sur la recherche vectorielle RAG exposerait les documents confidentiels d'un client à un autre — incident RGPD, pas juste un bug ; (2) l'isolation physique est un argument de vente direct pour une cible PME/ETI française sensible RGPD ("base dédiée, pas mutualisée") ; (3) pas de budget temps pour la réécriture (tenant_id partout, RLS Postgres, filtrage vectoriel, tests d'isolation) sur un produit qui tourne déjà, seul/petite équipe. Seuil de bascule vers le vrai multi-tenant : quand le coût marginal d'une instance (~35-60€/mois Render) dépasse ce qui est facturé aux plus petits clients, ou typiquement 15-25 clients pour une seule personne à opérer. Colonne `tenant_id` posée dès maintenant en préparation (cf. "Fait" ci-dessus) pour que cette bascule future soit une fusion de données, pas une réécriture de schéma. `render.yaml` est déjà en région Frankfurt (UE) — le discours commercial doit dire "hébergé en UE, conforme RGPD", pas "en France" (pas de datacenter Render en France ; OVH/Scaleway à envisager seulement si un client l'exige contractuellement, ex. secteur public).

### À faire — infrastructure de vente
Plan détaillé (décisions à trancher, provisioning + sous-domaine + mise à jour de masse + panel, risques identifiés) : `docs/FLEET_PROVISIONING_PLAN.md`
- [ ] Valider `ops/provision_client.py`/`ops/update_all_instances.py`/`ops/delete_client.py` contre un vrai compte Render (Phase 0 puis Phase 4 de `docs/FLEET_PROVISIONING_PLAN.md`) : token API généré, premier appel réel pour confirmer la forme exacte des payloads Render, provisioning d'une instance de test jetable de bout en bout (DB migrée, services accessibles, domaine + TLS si utilisés), test réel d'un cycle backup/restore — avant tout client réel. Scripts déjà écrits, cf. "Fait" ci-dessus
- [ ] Page `/setup?key=...` pour que le client choisisse lui-même son mot de passe admin sans qu'aucun mot de passe ne transite en clair côté vendeur (Phase 2 de `docs/FLEET_PROVISIONING_PLAN.md`) — `provision_client.py` affiche pour l'instant un mot de passe temporaire en attendant
- [ ] Panel interne (côté vendeur, pas client-facing) : liste des instances, statut santé (ping du `GET /` déjà utilisé comme `healthCheckPath` Render, réutilisable tel quel), plan tarifaire, date de facturation, lien vers les logs Render — CRUD Next.js + table Postgres séparée suffisent au démarrage. Le coupe-circuit par instance existe déjà (`GET`/`PUT /v1/instance/subscription-status`, cf. "Fait" ci-dessus) : ce panel n'aurait qu'à l'appeler à distance pour chaque client, pas à réinventer la logique de suspension. **Pas encore nécessaire** : à 1-5 clients, `ops/` (scripts CLI + `sqlite3 ops/instances.db "SELECT ..."`) suffit — ne construire cette interface que si la gestion en CLI devient réellement pénible
- [ ] Sous-domaine par client (`client-x.smartticket.fr`) via wildcard DNS + génération auto de certificat — sinon l'onboarding devient manuel à chaque nouveau client

### À faire — avant de pouvoir vendre (ordre de priorité réel)
- [ ] Relecture juridique du DPA/CGV/politique de confidentialité/mentions légales par un avocat spécialisé RGPD/SaaS avant tout usage contractuel réel + complétion des placeholders `[À COMPLÉTER]` (identité légale) une fois l'entité enregistrée — brouillons déjà rédigés, cf. "Fait" ci-dessus (`DPA.md` + les 3 pages publiques)
- [ ] Facturation automatisée (Stripe Billing, gestion TVA FR/UE) — pas de facturation manuelle dès le 3e client
- [ ] SLA écrit même basique (ex: 99% uptime, réponse support sous 24h) — demandé par les PME françaises dès qu'il y a du support client en jeu
- [ ] Process d'onboarding documenté et chronométré sur le premier client pilote : ingestion initiale de la base de connaissances, configuration des rôles, formation SAV — souvent le vrai facteur limitant de scale, pas la techno
- [ ] 1-2 clients pilotes réels avant tarif public, en échange de retours et testimonial

### Pricing envisagé (à valider avant fixation définitive)
Structure à 3 paliers indexée sur le volume de tickets IA traités (coût variable réel = tokens Mistral + stockage vectoriel) et le nombre d'agents SAV : Starter (~99-149€/mois, 1 agent, ~500 tickets IA/mois), Pro (~299-499€/mois, 3-5 agents, tickets illimités raisonnables), Business (sur devis 800€+/mois, agents illimités, SLA, DPA, onboarding assisté). Facture annuelle avec 15-20% de remise pour la trésorerie. **Point d'attention** : le coût infra fixe par client (~40-60€/mois d'instance Render) doit rester une fraction minoritaire du prix Starter, sinon la marge sur les petits comptes est nulle — à vérifier avant de fixer le prix plancher définitif.



### Email transactionnel (Brevo) — statut et reste à faire

**Débloqué pour le test (2026-08-14)** : le 401 intermittent venait de la restriction d'IP d'envoi côté Brevo (compte gratuit, IP mutualisée, IP résidentielle qui change). Résolu en désactivant le filtrage d'IP pour les clés API (Brevo → Sécurité). `SMTP_FROM=gabriel.guery10@gmail.com` (seule adresse validée dans Brevo → Senders). L'email de bienvenue part désormais correctement.

**À faire pour la production (bloquant) :**
- [ ] Acheter le domaine `smartticket.fr` (nécessaire aussi pour les sous-domaines par instance).
- [ ] Expéditeur sur domaine authentifié : `noreply@smartticket.fr` avec SPF + DKIM + DMARC configurés dans Brevo — règle à la fois le 401 IP, le spam Gmail (avertissement DMARC « Freemail non recommandé » constaté) et l'exigence Google/Yahoo/Microsoft.
- [ ] Envoi depuis une IP stable : quand le provisioning tournera sur un service hébergé (Render), l'IP d'envoi sera celle du serveur → plus de reconfirmation. Whitelister cette IP sortante plutôt que de tout ouvrir.
- [ ] Mettre à jour `SMTP_FROM` (partagé backend d'instance + `ops/notify.py`) vers l'adresse du domaine authentifié.
- [ ] Vérifier la délivrabilité réelle (mail reçu en boîte principale, pas en spam) avant le 1er client.

**Posture visée** : la sécurité vient de l'authentification du domaine (SPF/DKIM/DMARC) + de la clé API, pas du filtrage d'IP résidentielle. Ne pas s'appuyer sur la restriction d'IP comme garde-fou de fiabilité (une IP qui change casse l'envoi), mais ne pas tout laisser ouvert non plus : IP serveur stable + domaine authentifié.